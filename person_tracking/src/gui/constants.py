"""GUI 刷新节流参数配置

集中定义 GUI 组件的刷新频率，避免散落写死。
"""

# ========================================================================
# 刷新节流参数 (单位: 帧数)
# ========================================================================

# 目标表格刷新间隔 (每 N 帧刷新一次)
TARGET_TABLE_REFRESH_INTERVAL = 5  # 约每 5 帧刷新一次表格

# 轨迹列表刷新间隔 (每 N 帧刷新一次)
TRAJECTORY_LIST_REFRESH_INTERVAL = 10  # 约每 10 帧刷新一次轨迹

# 日志面板刷新间隔 (每 N 帧刷新一次)
LOG_PANEL_REFRESH_INTERVAL = 30  # 约每 30 帧刷新一次日志

# 状态栏 FPS 更新间隔 (秒)
STATUS_BAR_FPS_UPDATE_INTERVAL = 0.5  # 0.5 秒更新一次 FPS 显示

# ========================================================================
# 视频显示参数
# ========================================================================

# 实时预览模式: 是否使用轻量显示
# 轻量模式只显示: 边界框 + Track ID
# 完整模式还显示: 轨迹 + 置信度 + 中心点
LIGHTWEIGHT_PREVIEW = True

# 视频显示缩放质量
# Fast: 无平滑变换，速度快
# Smooth: 平滑变换，质量好但稍慢
SCALE_QUALITY = "Fast"  # "Fast" or "Smooth"

# 最大轨迹点显示数
MAX_TRAJECTORY_POINTS = 50

# ========================================================================
# 视频捕获参数
# ========================================================================

# 默认捕获帧率
DEFAULT_CAPTURE_FPS = 30.0

# 摄像头超时重试次数
CAMERA_RETRY_COUNT = 3

# 视频流重试间隔 (秒)
STREAM_RETRY_INTERVAL = 1.0

# ========================================================================
# 导出参数
# ========================================================================

# 默认输出目录
DEFAULT_OUTPUT_DIR = "output"

# CSV 导出文件名前缀
CSV_FILENAME_PREFIX = "tracking_log"

# 视频导出文件名前缀
VIDEO_FILENAME_PREFIX = "tracked"

# ========================================================================
# 其他参数
# ========================================================================

# 模拟帧率 (用于 Mock 演示)
MOCK_FPS = 30.0

# 模拟帧生成间隔 (秒)
MOCK_FRAME_INTERVAL = 1.0 / MOCK_FPS