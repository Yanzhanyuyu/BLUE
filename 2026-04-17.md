# 2026-04-17 更新记录

## 1. 本次改造目标
- 按真实 GUI 入口收敛调用链：`run_gui.py → src/gui/app.py → src/gui/main_window.py`
- 修复 MainWindow 与 worker 之间的双路径冲突
- 统一配置来源和 tracker 配置加载
- 补齐导出链路
- 增加回归测试验证

## 2. 核心修复点
- **唯一推理路径**
  - 摄像头定时器改为纯预览，不再在 GUI 线程中做推理
  - 推理统一由 `InferenceWorker` 处理，避免 GUI/worker 并发争用同一 pipeline
- **重复帧推理修复**
  - `InferenceWorker` 现在原子读取并清空最新帧
  - 加入 `last_processed_frame_id`，避免无新帧时重复处理旧帧
- **UI 参数与运行时真实绑定**
  - 修复 `_apply_ui_config_to_pipeline_config()` 为空实现的问题
  - confidence、iou、可视化开关等 UI 参数实际写入运行配置
- **导出链补齐**
  - 实现 CSV 导出
  - 实现 `trajectory_stats.json` 导出
  - 处理空数据提示逻辑
- **配置来源收口**
  - `load_config()` 默认读取仓库 `config/default.yaml`
  - 明确 `tracker_config_path` 为项目内 `config/bytetrack.yaml`
  - 修正 tracker 配置路径解析，避免传递模糊字符串

## 3. 运行时确认
- GUI 真实入口已确认：`run_gui.py → src/gui/app.py → MainWindow`
- `src/gui/controller.py` 经确认无运行时引用，可视为废弃兼容壳
- 真实工作链路已收敛，不存在“GUI 线程推理 + worker 推理”两条并行路径

## 4. 验证结果
- 关键回归测试：新增测试通过
- 相关回归测试：通过
- 运行验证点：
  - 摄像头预览仅显示，不再推理
  - worker 启动后不重复处理旧帧
  - 导出生成有效 CSV/JSON
  - UI 参数改动能影响运行参数
  - 配置默认来源为仓库 YAML

## 5. 仍需注意
- 视频导出仍未实现，暂不支持
- 进度条当前为只读显示，不支持 seek
- 以下参数仍需重新开始后才能生效：
  - `device`
  - `tracker_type`
  - `track_buffer`
