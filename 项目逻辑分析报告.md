# 人员检测跟踪系统 - 逻辑关系与问题分析报告

## 📋 文档概述

**项目名称**: 人员检测跟踪系统 (Person Tracking System)
**分析日期**: 2026-04-16
**项目版本**: 基于最新代码
**分析范围**: 整体架构、模块逻辑、数据流、潜在问题

---

## 一、项目整体架构

### 1.1 技术栈

| 技术类别 | 技术选型 | 用途 |
|---------|---------|------|
| **核心算法** | YOLOv11 | 目标检测 |
| **跟踪算法** | ByteTrack | 多目标跟踪 |
| **GUI框架** | PySide6 (Qt6) | 图形用户界面 |
| **视频处理** | OpenCV | 视频加载/保存/显示 |
| **配置管理** | Pydantic | 配置验证 |
| **日志系统** | Loguru | 日志记录 |
| **编程语言** | Python 3.10+ | 主要开发语言 |

### 1.2 五层架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                    GUI 层 (gui/)                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────┐  │
│  │   Views     │ │ Controllers │ │  Services   │ │Workers│ │
│  │  (视图层)   │◄│  (控制层)   │◄│  (服务层)   │◄│(工作线程)│ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────┘  │
├─────────────────────────────────────────────────────────────┤
│                    应用层 (main.py)                          │
│              CLI 入口、Python API、参数处理                   │
├─────────────────────────────────────────────────────────────┤
│                    核心层 (core/)                            │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │  Detector   │ │   Tracker    │ │   Pipeline   │          │
│  │  (检测器)   │ │  (跟踪器)    │ │  (处理管道)  │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
├─────────────────────────────────────────────────────────────┤
│                    数据层 (data/)                            │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │  VideoLoader│ │   Types      │ │ Trajectory   │          │
│  │  (视频加载) │ │  (数据类型)  │ │  (轨迹管理)  │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
├─────────────────────────────────────────────────────────────┤
│                  基础设施层 (infra/)                        │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │   Config    │ │   Logger    │ │  Exceptions  │          │
│  │  (配置管理) │ │  (日志系统)  │ │  (异常处理)  │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 目录结构

```
person_tracking/
├── config/                    # 配置文件
│   ├── default.yaml           # 默认配置
│   └── bytetrack.yaml         # ByteTrack 配置
├── src/                       # 源代码
│   ├── main.py                # CLI 入口 + run_tracking API
│   ├── __init__.py            # 包导出
│   ├── core/                  # 核心模块
│   │   ├── detector.py        # PersonDetector
│   │   ├── tracker.py         # PersonTracker, YOLOTrackerWrapper
│   │   └── pipeline.py       # TrackingPipeline
│   ├── data/                  # 数据模块
│   │   ├── loader.py          # VideoLoader, VideoWriter
│   │   ├── types.py           # BoundingBox, Detection, TrackedObject, Frame, Trajectory
│   │   └── trajectory.py      # TrajectoryManager
│   ├── viz/                   # 可视化模块
│   │   └── visualizer.py      # Visualizer
│   ├── export/                # 导出模块
│   │   └── csv_exporter.py    # CSVExporter
│   ├── infra/                 # 基础设施
│   │   ├── config.py          # Config, DetectorConfig, TrackerConfig...
│   │   ├── logger.py          # setup_logger, get_logger
│   │   ├── exceptions.py      # TrackingSystemError, ModelLoadError...
│   │   └── metrics.py         # MetricsCollector
│   └── gui/                   # GUI 模块
│       ├── app.py             # 应用入口
│       ├── main_window.py     # MainWindow
│       ├── workers.py         # VideoCaptureWorker, InferenceWorker
│       ├── controller.py      # Controller
│       ├── constants.py       # 常量定义
│       └── widgets/           # 可复用组件
│           ├── video_canvas.py
│           ├── trajectory_list.py
│           └── log_panel.py
├── tests/                     # 测试
│   ├── test_types.py
│   ├── test_config.py
│   ├── test_trajectory.py
│   ├── test_csv_exporter.py
│   ├── test_parameter_override.py
│   ├── test_worker_integration.py
│   └── test_export_functionality.py
├── scripts/                   # 工具脚本
│   └── verify_api_export.py   # API 导出验证
├── docs/                      # 文档
│   ├── PROJECT_HANDOFF.md
│   ├── GUI_DESIGN_HANDOFF.md
│   └── SESSION_HANDOFF.md
├── output/                    # 输出目录
├── logs/                      # 日志目录
├── requirements.txt           # 核心依赖
├── requirements-gui.txt      # GUI 依赖
├── run_gui.py                # GUI 启动脚本
├── run_gui.bat               # Windows GUI 启动脚本
├── AGENTS.md                 # 项目规则文件
└── README.md                 # 项目说明
```

---

## 二、核心业务逻辑分析

### 2.1 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                        视频源输入                              │
│  (视频文件 / 摄像头 / RTSP流)                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    VideoLoader                                │
│  - 加载视频/摄像头流                                           │
│  - 逐帧读取                                                   │
│  - 输出: Frame(frame_id, timestamp, image)                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    TrackingPipeline                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  1. YOLOv11 检测                                       │   │
│  │     - model.track() 一体化检测+跟踪                    │   │
│  │     - 输出: Detection(bbox, class_id, confidence)      │   │
│  └──────────────────────────────────────────────────────┘   │
│                              │                                 │
│                              ▼                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  2. ByteTrack 跟踪                                    │   │
│  │     - 跨帧关联 track_id                               │   │
│  │     - 输出: TrackedObject(track_id, detection, ...)   │   │
│  └──────────────────────────────────────────────────────┘   │
│                              │                                 │
│                              ▼                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  3. TrajectoryManager 更新                            │   │
│  │     - 添加轨迹点到历史记录                             │   │
│  │     - 清理失效轨迹                                     │   │
│  └──────────────────────────────────────────────────────┘   │
│                              │                                 │
│                              ▼                                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  4. Visualizer 渲染                                    │   │
│  │     - 绘制边界框、轨迹、ID标签                         │   │
│  │     - 输出: annotated_frame                            │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  VideoWriter    │ │  CSVExporter    │ │  GUI Display    │
│  (保存视频)     │ │  (导出日志)     │ │  (实时显示)     │
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 2.2 核心类关系图

```
┌─────────────────────────────────────────────────────────────┐
│                        Config                                │
│  (配置管理 - Pydantic BaseModel)                             │
│  - detector: DetectorConfig                                  │
│  - tracker: TrackerConfig                                    │
│  - visualizer: VisualizerConfig                              │
│  - pipeline: PipelineConfig                                  │
│  - logging: LoggingConfig                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    TrackingPipeline                          │
│  (处理管道 - 核心协调器)                                      │
│  - config: Config                                            │
│  - model: YOLO                                              │
│  - visualizer: Visualizer                                   │
│  - trajectory_manager: TrajectoryManager                    │
│  - _frame_cache: FrameStateCache                             │
│  - _metrics_collector: MetricsCollector                     │
│                                                              │
│  核心方法:                                                    │
│  - process_frame(): 处理单帧                                 │
│  - run(): 运行完整流程                                       │
│  - warmup(): 模型预热                                        │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│  Visualizer     │ │ TrajectoryManager│ │ MetricsCollector│
│  (可视化渲染)   │ │  (轨迹管理)     │ │  (性能指标)     │
│                 │ │                 │ │                 │
│  - render()     │ │ - update()      │ │ - report_frame()│
│  - draw_info()  │ │ - cleanup()     │ │ - get_snapshot()│
└─────────────────┘ └─────────────────┘ └─────────────────┘
```

### 2.3 数据类型层次

```
┌─────────────────────────────────────────────────────────────┐
│                    BoundingBox                               │
│  (边界框 - dataclass)                                         │
│  - x, y: 左上角坐标                                          │
│  - w, h: 宽高                                                │
│  - confidence: 置信度 [0, 1]                                │
│                                                              │
│  方法:                                                        │
│  - center: 计算中心点                                        │
│  - area: 计算面积                                            │
│  - iou(): 计算IOU                                           │
│  - to_xyxy() / from_xyxy(): 格式转换                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Detection                                 │
│  (检测结果 - dataclass)                                       │
│  - bbox: BoundingBox                                        │
│  - class_id: 类别ID (person=0)                               │
│  - class_name: 类别名称                                      │
│  - track_id: 跟踪ID (可选)                                   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    TrackedObject                             │
│  (跟踪对象 - dataclass)                                       │
│  - track_id: 跟踪ID (全局唯一)                               │
│  - detection: Detection                                     │
│  - frame_id: 所在帧ID                                        │
│  - timestamp: 时间戳                                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Frame                                     │
│  (帧数据 - dataclass)                                         │
│  - frame_id: 帧ID                                           │
│  - timestamp: 时间戳                                        │
│  - image: BGR图像数组 (H, W, C)                              │
│  - detections: 检测结果列表                                  │
│  - tracked_objects: 跟踪对象列表                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Trajectory                                │
│  (轨迹历史 - dataclass)                                       │
│  - track_id: 跟踪ID                                         │
│  - points: 轨迹点列表 [(x, y, timestamp), ...]              │
│  - max_length: 最大轨迹点数量                               │
│                                                              │
│  方法:                                                        │
│  - add_point(): 添加轨迹点                                   │
│  - get_recent_points(): 获取最近N个点                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、关键模块逻辑详解

### 3.1 TrackingPipeline (核心处理管道)

#### 3.1.1 初始化流程

```python
TrackingPipeline.__init__(config)
    │
    ├─> _setup_components()
    │   │
    │   ├─> setup_logger()  # 初始化日志系统
    │   │
    │   ├─> YOLO(model_path)  # 加载YOLO模型
    │   │   └─> model.to(device)  # 设置设备 (auto/cuda/mps/cpu)
    │   │
    │   ├─> Visualizer(config.visualizer)  # 初始化可视化器
    │   │
    │   └─> TrajectoryManager(...)  # 初始化轨迹管理器
    │
    ├─> MetricsCollector()  # 初始化性能指标收集器
    │
    └─> FrameStateCache()  # 初始化帧状态缓存
```

#### 3.1.2 单帧处理流程

```python
process_frame(frame, enable_tracking=True, render=True)
    │
    ├─> [检测+跟踪] model.track() 或 model()
    │   │
    │   ├─> 输入: frame.image
    │   ├─> 参数: conf, iou, classes, imgsz, tracker
    │   └─> 输出: results (YOLO Results)
    │
    ├─> [结果转换] _convert_results(results, frame_id, timestamp)
    │   │
    │   ├─> 提取 boxes.xyxy, confidences, class_ids
    │   ├─> 提取 track_ids (跟踪模式) 或生成临时ID (仅检测)
    │   ├─> 创建 BoundingBox.from_xyxy()
    │   ├─> 创建 Detection(bbox, class_id, class_name, track_id)
    │   └─> 创建 TrackedObject(track_id, detection, frame_id, timestamp)
    │
    ├─> [轨迹更新] trajectory_manager.update(obj) for obj in tracked_objects
    │   │
    │   ├─> 获取或创建 Trajectory
    │   ├─> 添加轨迹点 (center_x, center_y, timestamp)
    │   └─> 更新最后更新帧
    │
    ├─> [清理失效] trajectory_manager.cleanup_inactive(frame_id)
    │   │
    │   ├─> 计算最小有效帧: current_frame_id - inactive_threshold
    │   ├─> 找出失效轨迹: last_update_frame < min_frame
    │   └─> 移除失效轨迹
    │
    ├─> [性能统计] _metrics_collector.report_frame()
    │   │
    │   ├─> 记录帧ID、推理时间、检测数量
    │   └─> 计算FPS (移动平均)
    │
    └─> [可视化渲染] visualizer.render() + visualizer.draw_info()
        │
        ├─> render(image, tracked_objects, trajectories)
        │   ├─> 绘制边界框 (如果 show_bbox=True)
        │   ├─> 绘制轨迹线 (如果 show_trajectory=True)
        │   └─> 绘制ID标签 (如果 show_id=True)
        │
        └─> draw_info(annotated, frame_id, fps, detection_count)
            └─> 绘制帧信息 (帧号、FPS、检测数)
```

#### 3.1.3 完整运行流程

```python
run(source, output_path, csv_path, show, progress_callback)
    │
    ├─> warmup()  # 模型预热 (虚拟推理)
    │
    ├─> VideoLoader(source)  # 打开视频源
    │   └─> 获取: total_frames, fps, width, height
    │
    ├─> VideoWriter(output_path)  # 创建输出写入器
    │
    ├─> CSVExporter(csv_path)  # 创建CSV导出器
    │
    ├─> [重置状态]
    │   ├─> trajectory_manager.clear()
    │   ├─> visualizer.clear_color_cache()
    │   └─> _frame_cache.clear()
    │
    ├─> [逐帧处理循环]
    │   │
    │   ├─> for frame in loader:
    │   │   │
    │   │   ├─> [跳帧判断]
    │   │   │   │
    │   │   │   ├─> should_process = (skip_frames == 0) or
    │   │   │   │   │                (frame_counter % (skip_frames + 1) == 1) or
    │   │   │   │   │                (frame_counter == 1)
    │   │   │   │
    │   │   │   ├─> if should_process:
    │   │   │   │   │
    │   │   │   │   ├─> process_frame(frame, enable_tracking=True, render=False)
    │   │   │   │   │
    │   │   │   │   ├─> visualizer.render()  # 渲染可视化
    │   │   │   │   │
    │   │   │   │   ├─> visualizer.draw_info()  # 绘制信息
    │   │   │   │   │
    │   │   │   │   └─> _frame_cache.update()  # 更新缓存
    │   │   │   │
    │   │   │   └─> else:  # 跳过的帧
    │   │   │       │
    │   │   │       ├─> cached_frame = _frame_cache.get_last_annotated_frame()
    │   │   │       │
    │   │   │       └─> annotated = cached_frame.copy()  # 使用上一帧渲染结果
    │   │   │
    │   │   ├─> writer.write(annotated)  # 写入输出视频
    │   │   │
    │   │   ├─> csv_exporter.write_batch()  # 导出CSV (仅处理过的帧)
    │   │   │
    │   │   └─> cv2.imshow()  # 实时显示 (如果 show=True)
    │   │
    │   └─> [清理资源]
    │       ├─> loader.release()
    │       ├─> writer.release()
    │       ├─> csv_exporter.close()
    │       └─> cv2.destroyAllWindows()
    │
    └─> [返回统计信息]
        └─> {
            total_frames,
            elapsed_time,
            avg_fps,
            trajectories_created
        }
```

### 3.2 FrameStateCache (帧状态缓存)

#### 3.2.1 设计目的

**问题**: 跳帧时，如果直接使用原始帧，会导致输出视频中跳过的帧没有标注，造成视觉不连续。

**解决方案**: 使用 `FrameStateCache` 缓存上一帧的渲染结果，跳过的帧复用上一帧的标注。

#### 3.2.2 缓存策略

```python
FrameStateCache
    │
    ├─> _last_tracked_objects: list  # 上一帧跟踪对象
    ├─> _last_trajectories: dict     # 上一帧轨迹字典
    ├─> _last_annotated_frame: np.ndarray  # 上一帧渲染结果
    ├─> _last_frame_id: int          # 上一帧ID
    ├─> _last_timestamp: float       # 上一帧时间戳
    └─> _frame_history: list         # 帧历史 (用于平滑)
```

#### 3.2.3 关键方法

```python
# 更新缓存
update(frame_id, timestamp, tracked_objects, trajectories, annotated_frame)
    │
    ├─> 复制并保存: tracked_objects, trajectories, annotated_frame
    ├─> 保存: frame_id, timestamp
    └─> 维护历史记录 (最多 max_cache_size 帧)

# 获取上一帧渲染结果
get_last_annotated_frame()
    └─> 返回: _last_annotated_frame.copy()

# 获取插值帧 (未使用，保留接口)
get_interpolated_frame(current_frame, visualizer, frame_id)
    │
    ├─> if _last_annotated_frame is not None:
    │   └─> return _last_annotated_frame.copy()  # 直接复用
    │
    └─> else:
        └─> return current_frame.copy()  # 降级处理
```

### 3.3 TrajectoryManager (轨迹管理器)

#### 3.3.1 数据结构

```python
TrajectoryManager
    │
    ├─> _trajectories: dict[int, Trajectory]  # 轨迹字典
    │   └─> {track_id: Trajectory(track_id, points, max_length)}
    │
    ├─> _last_update_frame: dict[int, int]  # 最后更新帧
    │   └─> {track_id: frame_id}
    │
    ├─> _current_frame_id: int  # 当前帧ID
    ├─> max_trajectory_length: int  # 单个轨迹最大长度
    └─> inactive_threshold: int  # 失效阈值 (帧数)
```

#### 3.3.2 核心方法

```python
# 更新轨迹
update(tracked_obj)
    │
    ├─> track_id = tracked_obj.track_id
    ├─> center = tracked_obj.center
    ├─> timestamp = tracked_obj.timestamp
    │
    ├─> if track_id not in _trajectories:
    │   └─> 创建新轨迹: Trajectory(track_id, max_length)
    │
    ├─> _trajectories[track_id].add_point(center_x, center_y, timestamp)
    │
    └─> _last_update_frame[track_id] = tracked_obj.frame_id

# 清理失效轨迹
cleanup_inactive(current_frame_id)
    │
    ├─> min_frame = current_frame_id - inactive_threshold
    │
    ├─> 找出失效轨迹:
    │   └─> [track_id for track_id, last_frame in _last_update_frame.items()
    │       if last_frame < min_frame]
    │
    └─> 移除失效轨迹:
        ├─> _trajectories.pop(track_id, None)
        └─> _last_update_frame.pop(track_id, None)

# 获取所有轨迹的最近N个点
get_all_recent_points(n=50)
    │
    └─> {
        track_id: traj.get_recent_points(n)
        for track_id, traj in _trajectories.items()
    }
```

### 3.4 Visualizer (可视化器)

#### 3.4.1 配置项

```python
VisualizerConfig
    │
    ├─> box_color: tuple[int, int, int]  # 边界框颜色 (BGR)
    ├─> text_color: tuple[int, int, int]  # 文本颜色 (BGR)
    ├─> trajectory_color: tuple[int, int, int]  # 轨迹颜色 (BGR)
    ├─> line_thickness: int  # 线条宽度
    ├─> trajectory_length: int  # 显示的轨迹点数量
    ├─> show_center_point: bool  # 是否显示中心点
    ├─> show_confidence: bool  # 是否显示置信度
    ├─> font_scale: float  # 字体缩放比例
    ├─> show_bbox: bool  # 是否显示边界框 (GUI开关)
    ├─> show_trajectory: bool  # 是否显示轨迹 (GUI开关)
    └─> show_id: bool  # 是否显示ID标签 (GUI开关)
```

#### 3.4.2 渲染流程

```python
render(image, tracked_objects, trajectories)
    │
    ├─> [绘制轨迹]
    │   │
    │   ├─> if show_trajectory:
    │   │   │
    │   │   ├─> for track_id, trajectory in trajectories.items():
    │   │   │   │
    │   │   │   ├─> points = trajectory.get_recent_points(trajectory_length)
    │   │   │   │
    │   │   │   └─> cv2.polylines()  # 绘制轨迹线
    │   │   │
    │   │   └─> if show_center_point:
    │   │       └─> cv2.circle()  # 绘制中心点
    │
    ├─> [绘制边界框和ID]
    │   │
    │   └─> if show_bbox:
    │       │
    │       ├─> for obj in tracked_objects:
    │       │   │
    │       │   ├─> bbox = obj.bbox
    │       │   │
    │       │   ├─> cv2.rectangle()  # 绘制边界框
    │       │   │
    │       │   └─> if show_id:
    │       │       │
    │       │       ├─> text = f"ID:{obj.track_id}"
    │       │       │
    │       │       ├─> if show_confidence:
    │       │       │   └─> text += f" {obj.confidence:.2f}"
    │       │       │
    │       │       └─> cv2.putText()  # 绘制文本
    │
    └─> return annotated_image

draw_info(image, frame_id, fps, detection_count)
    │
    ├─> text = f"Frame: {frame_id} | FPS: {fps:.1f} | Objects: {detection_count}"
    │
    ├─> cv2.putText()  # 绘制信息文本
    │
    └─> return image
```

### 3.5 GUI 架构 (PySide6)

#### 3.5.1 组件层次

```
MainWindow (主窗口)
    │
    ├─> VideoCanvas (视频画布)
    │   └─> 显示渲染后的视频帧
    │
    ├─> TrajectoryList (轨迹列表)
    │   └─> 显示所有轨迹的历史记录
    │
    ├─> LogPanel (日志面板)
    │   └─> 显示系统运行日志
    │
    ├─> VideoCaptureWorker (视频采集线程)
    │   └─> 独立线程采集视频帧
    │
    └─> InferenceWorker (推理线程)
        └─> 独立线程进行检测和跟踪
```

#### 3.5.2 信号-槽机制

```python
# VideoCaptureWorker 信号
frame_ready = Signal(Frame)  # 帧采集完成
error_occurred = Signal(str)  # 错误发生

# InferenceWorker 信号
inference_result_ready = Signal(Frame, list[TrackedObject])  # 推理结果
metrics_updated = Signal(PerformanceMetrics)  # 性能指标更新
error_occurred = Signal(str)  # 错误发生

# MainWindow 槽
_on_worker_frame_ready(Frame)  # 处理采集的帧
_on_inference_result_ready(Frame, list[TrackedObject])  # 处理推理结果
_on_metrics_updated(PerformanceMetrics)  # 更新性能指标
_on_worker_error(str)  # 处理错误
```

#### 3.5.3 GUI 启动流程

```python
run_gui()
    │
    ├─> QApplication(sys.argv)  # 创建应用
    │
    ├─> MainWindow()  # 创建主窗口
    │   │
    │   ├─> setup_ui()  # 设置UI
    │   │
    │   ├─> setup_connections()  # 连接信号-槽
    │   │
    │   └─> show()  # 显示窗口
    │
    └─> app.exec()  # 运行事件循环
```

---

## 四、潜在问题分析

### 4.1 架构层面问题

#### 问题 1: Pipeline 直接调用 YOLO.track() 而非使用 PersonDetector

**位置**: `src/core/pipeline.py:269-278`

**问题描述**:
```python
# Pipeline 直接调用 YOLO.track()
results = self.model.track(
    frame.image,
    conf=self.config.detector.confidence_threshold,
    iou=self.config.detector.iou_threshold,
    classes=self.config.detector.classes,
    imgsz=self.config.detector.imgsz,
    tracker=f"{self.config.tracker.tracker_type}.yaml",
    persist=True,
    verbose=False,
)
```

虽然项目中存在 `PersonDetector` 类，但 `TrackingPipeline` 直接使用 `YOLO.track()` 进行一体化检测和跟踪，绕过了 `PersonDetector`。

**影响**:
- ✅ **优点**: 性能优化，避免重复计算
- ⚠️ **缺点**: 职责边界模糊，违反单一职责原则
- ⚠️ **缺点**: `PersonDetector` 类形同虚设，造成代码冗余

**设计决策说明**:
这是经过权衡后的设计决策。虽然违反了单一职责原则，但带来了显著的性能提升：
- 避免了检测和跟踪之间的数据拷贝
- 减少了模型推理次数
- 简化了代码流程

**建议**:
- **短期**: 保持现状，这是性能优化的设计决策
- **长期**: 考虑重构，明确职责边界，但需要评估性能影响

#### 问题 2: GUI 层直接访问核心层私有成员

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
```python
# 错误示例 (已修复)
for track_id, traj in self._pipeline.trajectory_manager._trajectories.items():
    trajectories[track_id] = traj.points[-50:]
```

**影响**:
- ❌ 破坏封装性
- ❌ 依赖内部实现细节
- ❌ 代码维护困难

**修复方案**:
```python
# 正确做法
trajectories = self._pipeline.trajectory_manager.get_all_recent_points(50)
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 3: 参数覆盖使用 truthy 判断

**位置**: `src/main.py` (已修复)

**问题描述**:
```python
# 错误示例 (已修复)
if confidence:
    cfg.detector.confidence_threshold = confidence
```

当 `confidence=0.0` 时，会被错误跳过。

**影响**:
- ❌ 边缘值处理错误
- ❌ 用户无法设置 0.0 置信度

**修复方案**:
```python
# 正确做法
if confidence is not None:
    cfg.detector.confidence_threshold = confidence
```

**状态**: ✅ 已修复 (2026-04-14)

### 4.2 性能层面问题

#### 问题 4: 跳帧时视觉不连续

**位置**: `src/core/pipeline.py:523-534`

**问题描述**:
跳过的帧如果没有缓存，会显示原始帧，导致输出视频中标注闪烁。

**影响**:
- ⚠️ 视觉体验差
- ⚠️ 轨迹断裂

**修复方案**:
```python
# 使用 FrameStateCache 缓存上一帧渲染结果
cached_frame = self._frame_cache.get_last_annotated_frame()
if cached_frame is not None:
    annotated = cached_frame.copy()
elif last_annotated_frame is not None:
    annotated = last_annotated_frame.copy()
else:
    annotated = frame.image.copy()  # 降级处理
```

**状态**: ✅ 已修复 (2026-04-15)

#### 问题 5: FPS 多源计算问题

**位置**: `src/core/pipeline.py:307-315`

**问题描述**:
之前存在多个地方计算FPS，导致显示不一致。

**影响**:
- ⚠️ FPS显示不准确
- ⚠️ 性能指标混乱

**修复方案**:
```python
# 使用统一的 MetricsCollector
self._metrics_collector.report_frame(
    frame_id=frame.frame_id,
    inference_time_ms=inference_time_ms,
    detection_count=len(tracked_objects)
)

# 获取统一计算的性能指标
metrics = self._metrics_collector.get_snapshot()
```

**状态**: ✅ 已修复

### 4.3 数据层面问题

#### 问题 6: BoundingBox 无输入验证

**位置**: `src/data/types.py:49-65`

**问题描述**:
之前 `BoundingBox` 类没有输入验证，可能导致无效数据。

**影响**:
- ❌ 负数宽高
- ❌ 超出范围的置信度
- ❌ 非数值类型

**修复方案**:
```python
def __post_init__(self) -> None:
    """验证输入参数"""
    # 验证宽度和高度
    if self.w < 0:
        raise ValueError(f"宽度 w 不能为负数，当前值: {self.w}")
    if self.h < 0:
        raise ValueError(f"高度 h 不能为负数，当前值: {self.h}")

    # 验证置信度范围
    if not 0.0 <= self.confidence <= 1.0:
        raise ValueError(
            f"置信度 confidence 必须在 [0, 1] 范围内，当前值: {self.confidence}"
        )

    # 验证坐标是否为数值
    if not all(isinstance(v, (int, float)) for v in [self.x, self.y, self.w, self.h, self.confidence]):
        raise ValueError("所有坐标和尺寸参数必须是数值类型")
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 7: CSV 无原子写入

**位置**: `src/export/csv_exporter.py` (已修复)

**问题描述**:
直接写入CSV文件，如果程序崩溃可能导致文件损坏。

**影响**:
- ❌ 数据丢失
- ❌ 文件损坏

**修复方案**:
```python
# 使用临时文件+重命名机制
temp_path = f"{self._csv_path}.tmp"
with open(temp_path, 'w', newline='', encoding='utf-8') as f:
    writer = csv.writer(f)
    writer.writerow(self._header)
    for obj in self._records:
        writer.writerow(self._format_record(obj))

# 原子重命名
os.replace(temp_path, self._csv_path)
```

**状态**: ✅ 已修复 (2026-04-14)

### 4.4 GUI 层面问题

#### 问题 8: GUI 主线程阻塞

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
之前视频采集和推理在GUI主线程执行，导致界面卡顿。

**影响**:
- ❌ 界面无响应
- ❌ 用户体验差

**修复方案**:
```python
# 使用独立Worker线程
self._video_worker = VideoCaptureWorker(source)
self._inference_worker = InferenceWorker(pipeline)

# 连接信号
self._video_worker.frame_ready.connect(self._on_worker_frame_ready)
self._inference_worker.inference_result_ready.connect(self._on_inference_result_ready)
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 9: 视频画面位置跳动

**位置**: `src/gui/widgets/video_canvas.py` (已修复)

**问题描述**:
视频画面在居中和左上角之间跳动。

**影响**:
- ⚠️ 视觉体验差
- ⚠️ 画面不稳定

**修复方案**:
```python
# 从布局中移除 _image_label，改为直接子 widget
# 在 _update_display() 中添加显式居中逻辑
def _update_display(self, image: np.ndarray):
    # ... 缩放图像 ...

    # 显式居中
    x = (self.width() - self._image_label.width()) // 2
    y = (self.height() - self._image_label.height()) // 2
    self._image_label.move(x, y)
```

**状态**: ✅ 已修复 (2026-04-15)

#### 问题 10: GUI 显示开关不生效

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
GUI的显示开关（边界框、轨迹、ID）不生效，因为Visualizer没有对应的配置项。

**影响**:
- ❌ 用户无法控制显示内容
- ❌ 功能不可用

**修复方案**:
```python
# 1. 在 VisualizerConfig 中添加配置项
class VisualizerConfig(BaseModel):
    show_bbox: bool = Field(default=True, description="是否显示边界框")
    show_trajectory: bool = Field(default=True, description="是否显示轨迹")
    show_id: bool = Field(default=True, description="是否显示ID标签")

# 2. 在 Visualizer.render() 中使用配置
if self.config.show_bbox:
    # 绘制边界框

if self.config.show_trajectory:
    # 绘制轨迹

if self.config.show_id:
    # 绘制ID标签

# 3. 在 MainWindow 中同步UI配置到Pipeline配置
def _apply_ui_config_to_pipeline_config(self):
    self._pipeline.config.visualizer.show_bbox = self._show_bbox_checkbox.isChecked()
    self._pipeline.config.visualizer.show_trajectory = self._show_trajectory_checkbox.isChecked()
    self._pipeline.config.visualizer.show_id = self._show_id_checkbox.isChecked()
```

**状态**: ✅ 已修复 (2026-04-15)

### 4.5 配置层面问题

#### 问题 11: show_progress 配置未使用

**位置**: `src/main.py` (已修复)

**问题描述**:
`show_progress` 配置项存在但未生效。

**影响**:
- ❌ 配置项无效
- ❌ 用户无法控制进度条显示

**修复方案**:
```python
# 在 CLI 模式中使用配置
should_show_progress = not quiet and cfg.pipeline.show_progress

if should_show_progress:
    # 显示进度条
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 12: device 默认为 cuda

**位置**: `src/infra/config.py:59-62` (已修复)

**问题描述**:
`device` 默认为 `cuda`，在没有CUDA的机器上会报错。

**影响**:
- ❌ 在无GPU机器上无法运行
- ❌ 用户体验差

**修复方案**:
```python
# 改为 auto 自动选择
device: Literal["cuda", "cpu", "mps", "auto"] = Field(
    default="auto",  # 改为auto自动选择
    description="推理设备（auto自动选择）",
)

# 添加验证器自动选择设备
@field_validator('device', mode='before')
@classmethod
def resolve_auto_device(cls, v: str) -> str:
    if v == 'auto':
        try:
            import torch
            if torch.cuda.is_available():
                return 'cuda'
            elif hasattr(torch.backends, 'mps') and torch.backends.mps.is_available():
                return 'mps'
            else:
                return 'cpu'
        except ImportError:
            return 'cpu'
    return v
```

**状态**: ✅ 已修复

### 4.6 测试层面问题

#### 问题 13: 测试覆盖不足

**位置**: `tests/` 目录

**问题描述**:
之前测试覆盖不足，特别是Worker集成测试和导出功能测试。

**影响**:
- ⚠️ 代码质量无法保证
- ⚠️ 回归风险高

**修复方案**:
```python
# 新增测试文件
- test_worker_integration.py  # Worker集成测试 (7个测试)
- test_export_functionality.py  # 导出功能测试 (12个测试)
- test_parameter_override.py  # 参数边界值测试 (19个测试)

# 测试总数从 65 增加到 91
```

**状态**: ✅ 已修复 (2026-04-14)

### 4.7 新发现的潜在问题

#### 问题 14: 内存泄漏 - Visualizer._color_map 无上限 🔴

**位置**: `src/viz/visualizer.py:44`

**问题描述**:
```python
def __init__(self, config: VisualizerConfig) -> None:
    self.config = config
    self._color_map = {}  # track_id -> color 缓存

def _get_color(self, track_id: int) -> tuple[int, int, int]:
    if track_id not in self._color_map:
        # 生成随机颜色（基于 track_id）
        np.random.seed(track_id % 1000)
        color = tuple(
            np.random.randint(50, 256, size=3).tolist()
        )
        self._color_map[track_id] = color  # 永久缓存，无清理机制
```

每个新 `track_id` 都会分配颜色并永久缓存，长时间运行内存持续增长。

**影响**:
- 🔴 长时间监控视频处理会耗尽内存
- 🔴 每个track_id占用约24字节（3个int），1000个track_id约24KB
- 🔴 如果track_id持续增长（如不清理失效轨迹），内存会无限增长

**修复建议**:
```python
# 方案1: 限制缓存大小
def __init__(self, config: VisualizerConfig) -> None:
    self.config = config
    self._color_map = {}  # track_id -> color 缓存
    self._max_cache_size = 1000  # 限制缓存大小

def _get_color(self, track_id: int) -> tuple[int, int, int]:
    if track_id not in self._color_map:
        # 检查缓存大小
        if len(self._color_map) >= self._max_cache_size:
            # 清理最旧的条目
            oldest_key = next(iter(self._color_map))
            del self._color_map[oldest_key]
        
        # 生成颜色
        np.random.seed(track_id % 1000)
        color = tuple(
            np.random.randint(50, 256, size=3).tolist()
        )
        self._color_map[track_id] = color
    
    return self._color_map[track_id]

# 方案2: 定期清理（配合TrajectoryManager的cleanup_inactive）
def clear_color_cache(self) -> None:
    """清除颜色缓存"""
    self._color_map.clear()
    logger.debug("Color cache cleared")
```

**状态**: ⚠️ 未修复，需要添加缓存大小限制

#### 问题 15: 线程安全 - GUI Worker数据共享无锁保护 🔴

**位置**: `src/gui/main_window.py:98`

**问题描述**:
```python
# Pipeline 被Worker线程和主线程共享
self._pipeline: Optional["TrackingPipeline"] = None

# Worker线程访问
def _on_inference_result_ready(self, frame, tracked_objects):
    # Worker线程访问 self._pipeline
    trajectories = self._pipeline.trajectory_manager.get_all_recent_points(50)

# 主线程也可能访问
def _on_config_value_changed(self, key, value):
    # 主线程访问 self._pipeline
    if self._pipeline:
        # 修改配置
        pass
```

`_pipeline` 被Worker线程和主线程共享，无显式同步机制。

**影响**:
- 🔴 竞态条件风险
- 🔴 数据竞争
- 🔴 可能导致崩溃或数据不一致

**修复建议**:
```python
# 方案1: 使用线程锁
from threading import Lock

class MainWindow(QMainWindow):
    def __init__(self, parent: Optional[QWidget] = None) -> None:
        super().__init__(parent)
        self._pipeline_lock = Lock()
        self._pipeline: Optional["TrackingPipeline"] = None

    def _on_inference_result_ready(self, frame, tracked_objects):
        with self._pipeline_lock:
            if self._pipeline:
                trajectories = self._pipeline.trajectory_manager.get_all_recent_points(50)
        # ... 处理 ...

    def _on_config_value_changed(self, key, value):
        with self._pipeline_lock:
            if self._pipeline:
                # 修改配置
                pass

# 方案2: 使用信号槽的自动线程安全（Qt已提供）
# Qt的信号槽机制已经保证了线程安全，但需要确保：
# 1. 所有跨线程数据访问都通过信号传递
# 2. 避免直接共享可变对象
```

**状态**: ⚠️ 未修复，需要添加线程同步机制

#### 问题 16: 资源泄漏 - Camera和Mock未正确清理 🔴

**位置**: `src/gui/main_window.py:90-96`

**问题描述**:
```python
# 摄像头相关
self._camera_capture: Optional["cv2.VideoCapture"] = None
self._camera_timer: Optional[QTimer] = None

# Mock 数据生成器
self._mock_generator: Optional[MockFrameGenerator] = None
self._mock_timer: Optional[QTimer] = None
```

清理逻辑分散，无统一资源管理，可能导致：
- 摄像头句柄未释放
- QTimer未停止
- 内存泄漏

**影响**:
- 🔴 摄像头被占用，无法重新打开
- 🔴 内存持续增长
- 🔴 程序崩溃风险

**修复建议**:
```python
# 统一资源管理
class MainWindow(QMainWindow):
    def __init__(self, parent: Optional[QWidget] = None) -> None:
        super().__init__(parent)
        self._resources = {
            'camera_capture': None,
            'camera_timer': None,
            'mock_generator': None,
            'mock_timer': None,
        }

    def _cleanup_resources(self) -> None:
        """统一清理所有资源"""
        # 停止所有定时器
        for timer in [self._resources['camera_timer'], self._resources['mock_timer']]:
            if timer:
                timer.stop()
                timer.deleteLater()
                timer = None

        # 释放摄像头
        camera = self._resources['camera_capture']
        if camera:
            camera.release()
            camera = None

        # 清理Mock生成器
        self._resources['mock_generator'] = None

        logger.debug("All resources cleaned up")

    def closeEvent(self, event):
        """窗口关闭时清理资源"""
        self._cleanup_resources()
        super().closeEvent(event)
```

**状态**: ⚠️ 未修复，需要统一资源管理

#### 问题 17: 配置验证缺失 - 颜色值未验证 🟡

**位置**: `src/infra/config.py:153-162`

**问题描述**:
```python
class VisualizerConfig(BaseModel):
    box_color: tuple[int, int, int] = Field(
        default=(0, 255, 0),
        description="边界框颜色 (BGR)",
    )
    text_color: tuple[int, int, int] = Field(
        default=(255, 255, 255),
        description="文本颜色 (BGR)",
    )
    trajectory_color: tuple[int, int, int] = Field(
        default=(255, 0, 0),
        description="轨迹颜色 (BGR)",
    )
```

颜色字段无范围验证，BGR值应在0-255之间。

**影响**:
- 🟡 用户可能输入无效颜色值
- 🟡 可能导致渲染错误

**修复建议**:
```python
from pydantic import field_validator

class VisualizerConfig(BaseModel):
    box_color: tuple[int, int, int] = Field(
        default=(0, 255, 0),
        description="边界框颜色 (BGR)",
    )
    
    @field_validator('box_color', 'text_color', 'trajectory_color', mode='before')
    @classmethod
    def validate_color(cls, v):
        """验证颜色值在0-255范围内"""
        if not isinstance(v, (list, tuple)) or len(v) != 3:
            raise ValueError("颜色必须是包含3个元素的列表或元组")
        
        for i, val in enumerate(v):
            if not isinstance(val, int) or not 0 <= val <= 255:
                raise ValueError(f"颜色值必须在0-255范围内，当前值: {val}")
        
        return tuple(v)
```

**状态**: ⚠️ 未修复，需要添加颜色验证

#### 问题 18: 异常处理不足 - Worker异常可能静默失败 🟡

**位置**: `src/gui/workers.py`

**问题描述**:
Worker线程中的异常通过信号传递，但主线程处理可能延迟或遗漏。

**影响**:
- 🟡 异常可能被静默忽略
- 🟡 用户无法及时感知错误

**修复建议**:
```python
# 在Worker中添加异常处理
class InferenceWorker(QThread):
    error_occurred = Signal(str)
    
    def run(self):
        try:
            # ... 推理逻辑 ...
        except Exception as e:
            logger.error(f"Inference worker error: {e}", exc_info=True)
            self.error_occurred.emit(f"推理错误: {str(e)}")
            # 确保异常被传播
            raise

# 在MainWindow中添加异常处理
def _on_worker_error(self, error_msg: str):
    """处理Worker错误"""
    logger.error(f"Worker error: {error_msg}")
    
    # 显示错误对话框
    QMessageBox.critical(
        self,
        "错误",
        f"处理过程中发生错误:\n{error_msg}",
        QMessageBox.StandardButton.Ok
    )
    
    # 停止处理
    self.stop_tracking()
```

**状态**: ⚠️ 部分修复，需要增强异常处理

#### 问题 19: CSV导出原子性限制 - Windows下非真正原子 🟡

**位置**: `src/export/csv_exporter.py`

**问题描述**:
文档声称"原子写入"，但代码中使用 `os.replace()` 在Windows上不是原子操作。

**影响**:
- 🟡 Windows下可能不是真正的原子操作
- 🟡 极端情况下可能文件损坏

**修复建议**:
```python
# Windows下的真正原子写入需要特殊处理
import os
import platform

def atomic_write_csv(self, data: str, output_path: str) -> None:
    """原子写入CSV文件"""
    temp_path = f"{output_path}.tmp"
    
    # 写入临时文件
    with open(temp_path, 'w', newline='', encoding='utf-8') as f:
        f.write(data)
    
    # 原子重命名
    if platform.system() == 'Windows':
        # Windows下使用MoveFileEx进行原子重命名
        import ctypes
        from ctypes import wintypes
        
        MOVEFILE_REPLACE_EXISTING = 0x1
        MOVEFILE_WRITE_THROUGH = 0x8
        
        success = ctypes.windll.kernel32.MoveFileExW(
            temp_path,
            output_path,
            MOVEFILE_REPLACE_EXISTING | MOVEFILE_WRITE_THROUGH
        )
        
        if not success:
            raise OSError(f"Failed to atomically replace file: {output_path}")
    else:
        # Unix-like系统下os.replace是原子的
        os.replace(temp_path, output_path)
```

**状态**: ⚠️ 未修复，需要增强Windows下的原子性

#### 问题 2: GUI 层直接访问核心层私有成员

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
```python
# 错误示例 (已修复)
for track_id, traj in self._pipeline.trajectory_manager._trajectories.items():
    trajectories[track_id] = traj.points[-50:]
```

**影响**:
- ❌ 破坏封装性
- ❌ 依赖内部实现细节
- ❌ 代码维护困难

**修复方案**:
```python
# 正确做法
trajectories = self._pipeline.trajectory_manager.get_all_recent_points(50)
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 3: 参数覆盖使用 truthy 判断

**位置**: `src/main.py` (已修复)

**问题描述**:
```python
# 错误示例 (已修复)
if confidence:
    cfg.detector.confidence_threshold = confidence
```

当 `confidence=0.0` 时，会被错误跳过。

**影响**:
- ❌ 边缘值处理错误
- ❌ 用户无法设置 0.0 置信度

**修复方案**:
```python
# 正确做法
if confidence is not None:
    cfg.detector.confidence_threshold = confidence
```

**状态**: ✅ 已修复 (2026-04-14)

### 4.2 性能层面问题

#### 问题 4: 跳帧时视觉不连续

**位置**: `src/core/pipeline.py:523-534`

**问题描述**:
跳过的帧如果没有缓存，会显示原始帧，导致输出视频中标注闪烁。

**影响**:
- ⚠️ 视觉体验差
- ⚠️ 轨迹断裂

**修复方案**:
```python
# 使用 FrameStateCache 缓存上一帧渲染结果
cached_frame = self._frame_cache.get_last_annotated_frame()
if cached_frame is not None:
    annotated = cached_frame.copy()
elif last_annotated_frame is not None:
    annotated = last_annotated_frame.copy()
else:
    annotated = frame.image.copy()  # 降级处理
```

**状态**: ✅ 已修复 (2026-04-15)

#### 问题 5: FPS 多源计算问题

**位置**: `src/core/pipeline.py:307-315`

**问题描述**:
之前存在多个地方计算FPS，导致显示不一致。

**影响**:
- ⚠️ FPS显示不准确
- ⚠️ 性能指标混乱

**修复方案**:
```python
# 使用统一的 MetricsCollector
self._metrics_collector.report_frame(
    frame_id=frame.frame_id,
    inference_time_ms=inference_time_ms,
    detection_count=len(tracked_objects)
)

# 获取统一计算的性能指标
metrics = self._metrics_collector.get_snapshot()
```

**状态**: ✅ 已修复

### 4.3 数据层面问题

#### 问题 6: BoundingBox 无输入验证

**位置**: `src/data/types.py:49-65`

**问题描述**:
之前 `BoundingBox` 类没有输入验证，可能导致无效数据。

**影响**:
- ❌ 负数宽高
- ❌ 超出范围的置信度
- ❌ 非数值类型

**修复方案**:
```python
def __post_init__(self) -> None:
    """验证输入参数"""
    # 验证宽度和高度
    if self.w < 0:
        raise ValueError(f"宽度 w 不能为负数，当前值: {self.w}")
    if self.h < 0:
        raise ValueError(f"高度 h 不能为负数，当前值: {self.h}")

    # 验证置信度范围
    if not 0.0 <= self.confidence <= 1.0:
        raise ValueError(
            f"置信度 confidence 必须在 [0, 1] 范围内，当前值: {self.confidence}"
        )

    # 验证坐标是否为数值
    if not all(isinstance(v, (int, float)) for v in [self.x, self.y, self.w, self.h, self.confidence]):
        raise ValueError("所有坐标和尺寸参数必须是数值类型")
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 7: CSV 无原子写入

**位置**: `src/export/csv_exporter.py` (已修复)

**问题描述**:
直接写入CSV文件，如果程序崩溃可能导致文件损坏。

**影响**:
- ❌ 数据丢失
- ❌ 文件损坏

**修复方案**:
```python
# 使用临时文件+重命名机制
temp_path = f"{self._csv_path}.tmp"
with open(temp_path, 'w', newline='', encoding='utf-8') as f:
    writer = csv.writer(f)
    writer.writerow(self._header)
    for obj in self._records:
        writer.writerow(self._format_record(obj))

# 原子重命名
os.replace(temp_path, self._csv_path)
```

**状态**: ✅ 已修复 (2026-04-14)

### 4.4 GUI 层面问题

#### 问题 8: GUI 主线程阻塞

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
之前视频采集和推理在GUI主线程执行，导致界面卡顿。

**影响**:
- ❌ 界面无响应
- ❌ 用户体验差

**修复方案**:
```python
# 使用独立Worker线程
self._video_worker = VideoCaptureWorker(source)
self._inference_worker = InferenceWorker(pipeline)

# 连接信号
self._video_worker.frame_ready.connect(self._on_worker_frame_ready)
self._inference_worker.inference_result_ready.connect(self._on_inference_result_ready)
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 9: 视频画面位置跳动

**位置**: `src/gui/widgets/video_canvas.py` (已修复)

**问题描述**:
视频画面在居中和左上角之间跳动。

**影响**:
- ⚠️ 视觉体验差
- ⚠️ 画面不稳定

**修复方案**:
```python
# 从布局中移除 _image_label，改为直接子 widget
# 在 _update_display() 中添加显式居中逻辑
def _update_display(self, image: np.ndarray):
    # ... 缩放图像 ...

    # 显式居中
    x = (self.width() - self._image_label.width()) // 2
    y = (self.height() - self._image_label.height()) // 2
    self._image_label.move(x, y)
```

**状态**: ✅ 已修复 (2026-04-15)

#### 问题 10: GUI 显示开关不生效

**位置**: `src/gui/main_window.py` (已修复)

**问题描述**:
GUI的显示开关（边界框、轨迹、ID）不生效，因为Visualizer没有对应的配置项。

**影响**:
- ❌ 用户无法控制显示内容
- ❌ 功能不可用

**修复方案**:
```python
# 1. 在 VisualizerConfig 中添加配置项
class VisualizerConfig(BaseModel):
    show_bbox: bool = Field(default=True, description="是否显示边界框")
    show_trajectory: bool = Field(default=True, description="是否显示轨迹")
    show_id: bool = Field(default=True, description="是否显示ID标签")

# 2. 在 Visualizer.render() 中使用配置
if self.config.show_bbox:
    # 绘制边界框

if self.config.show_trajectory:
    # 绘制轨迹

if self.config.show_id:
    # 绘制ID标签

# 3. 在 MainWindow 中同步UI配置到Pipeline配置
def _apply_ui_config_to_pipeline_config(self):
    self._pipeline.config.visualizer.show_bbox = self._show_bbox_checkbox.isChecked()
    self._pipeline.config.visualizer.show_trajectory = self._show_trajectory_checkbox.isChecked()
    self._pipeline.config.visualizer.show_id = self._show_id_checkbox.isChecked()
```

**状态**: ✅ 已修复 (2026-04-15)

### 4.5 配置层面问题

#### 问题 11: show_progress 配置未使用

**位置**: `src/main.py` (已修复)

**问题描述**:
`show_progress` 配置项存在但未生效。

**影响**:
- ❌ 配置项无效
- ❌ 用户无法控制进度条显示

**修复方案**:
```python
# 在 CLI 模式中使用配置
should_show_progress = not quiet and cfg.pipeline.show_progress

if should_show_progress:
    # 显示进度条
```

**状态**: ✅ 已修复 (2026-04-14)

#### 问题 12: device 默认为 cuda

**位置**: `src/infra/config.py:59-62` (已修复)

**问题描述**:
`device` 默认为 `cuda`，在没有CUDA的机器上会报错。

**影响**:
- ❌ 在无GPU机器上无法运行
- ❌ 用户体验差

**修复方案**:
```python
# 改为 auto 自动选择
device: Literal["cuda", "cpu", "mps", "auto"] = Field(
    default="auto",  # 改为auto自动选择
    description="推理设备（auto自动选择）",
)

# 添加验证器自动选择设备
@field_validator('device', mode='before')
@classmethod
def resolve_auto_device(cls, v: str) -> str:
    if v == 'auto':
        try:
            import torch
            if torch.cuda.is_available():
                return 'cuda'
            elif hasattr(torch.backends, 'mps') and torch.backends.mps.is_available():
                return 'mps'
            else:
                return 'cpu'
        except ImportError:
            return 'cpu'
    return v
```

**状态**: ✅ 已修复

### 4.6 测试层面问题

#### 问题 13: 测试覆盖不足

**位置**: `tests/` 目录

**问题描述**:
之前测试覆盖不足，特别是Worker集成测试和导出功能测试。

**影响**:
- ⚠️ 代码质量无法保证
- ⚠️ 回归风险高

**修复方案**:
```python
# 新增测试文件
- test_worker_integration.py  # Worker集成测试 (7个测试)
- test_export_functionality.py  # 导出功能测试 (12个测试)
- test_parameter_override.py  # 参数边界值测试 (19个测试)

# 测试总数从 65 增加到 91
```

**状态**: ✅ 已修复 (2026-04-14)

---

## 五、逻辑关系总结

### 5.1 模块依赖关系

```
main.py (CLI入口)
    │
    ├─> infra.config (配置管理)
    │   └─> Config, DetectorConfig, TrackerConfig, VisualizerConfig, PipelineConfig, LoggingConfig
    │
    ├─> core.pipeline (处理管道)
    │   │
    │   ├─> infra.config (配置)
    │   ├─> data.loader (视频加载)
    │   │   └─> VideoLoader, VideoWriter
    │   ├─> data.types (数据类型)
    │   │   └─> Frame, TrackedObject, PerformanceMetrics
    │   ├─> data.trajectory (轨迹管理)
    │   │   └─> TrajectoryManager
    │   ├─> viz.visualizer (可视化)
    │   │   └─> Visualizer
    │   ├─> export.csv_exporter (CSV导出)
    │   │   └─> CSVExporter
    │   ├─> infra.logger (日志)
    │   │   └─> setup_logger, get_logger
    │   └─> infra.metrics (性能指标)
    │       └─> MetricsCollector
    │
    └─> gui (GUI模块)
        │
        ├─> gui.app (应用入口)
        ├─> gui.main_window (主窗口)
        │   │
        │   ├─> gui.workers (工作线程)
        │   │   └─> VideoCaptureWorker, InferenceWorker
        │   │
        │   ├─> gui.widgets (组件)
        │   │   ├─> video_canvas (视频画布)
        │   │   ├─> trajectory_list (轨迹列表)
        │   │   └─> log_panel (日志面板)
        │   │
        │   └─> core.pipeline (处理管道)
        │
        └─> infra.config (配置)
```

### 5.2 数据流向

```
视频源
    │
    ▼
VideoLoader
    │
    ├─> Frame(frame_id, timestamp, image)
    │
    ▼
TrackingPipeline
    │
    ├─> [检测+跟踪] YOLO.track()
    │   │
    │   └─> results
    │
    ├─> [结果转换] _convert_results()
    │   │
    │   └─> TrackedObject[]
    │
    ├─> [轨迹更新] TrajectoryManager.update()
    │   │
    │   └─> Trajectory[]
    │
    ├─> [可视化] Visualizer.render()
    │   │
    │   └─> annotated_frame
    │
    ├─> [性能统计] MetricsCollector.report_frame()
    │   │
    │   └─> PerformanceMetrics
    │
    └─> [缓存] FrameStateCache.update()
        │
        └─> 缓存上一帧状态
```

### 5.3 配置传播路径

```
YAML配置文件 (config/default.yaml)
    │
    ▼
load_config()
    │
    ├─> Config (Pydantic验证)
    │   │
    │   ├─> detector: DetectorConfig
    │   ├─> tracker: TrackerConfig
    │   ├─> visualizer: VisualizerConfig
    │   ├─> pipeline: PipelineConfig
    │   └─> logging: LoggingConfig
    │
    ▼
TrackingPipeline(config)
    │
    ├─> self.config = config
    │
    ├─> self.model = YOLO(config.detector.model_path)
    │
    ├─> self.visualizer = Visualizer(config.visualizer)
    │
    └─> self.trajectory_manager = TrajectoryManager(
            max_trajectory_length=config.visualizer.trajectory_length,
            inactive_threshold=config.tracker.track_buffer,
        )
```

### 5.4 GUI 配置同步路径

```
UI控件 (复选框/滑块)
    │
    ▼
用户交互
    │
    ▼
MainWindow._on_config_value_changed()
    │
    ├─> config_changed 信号
    │
    ▼
MainWindow._apply_ui_config_to_pipeline_config()
    │
    ├─> self._pipeline.config.detector.confidence_threshold = ui_value
    ├─> self._pipeline.config.detector.iou_threshold = ui_value
    ├─> self._pipeline.config.detector.device = ui_value
    ├─> self._pipeline.config.tracker.track_buffer = ui_value
    ├─> self._pipeline.config.visualizer.show_bbox = ui_value
    ├─> self._pipeline.config.visualizer.show_trajectory = ui_value
    └─> self._pipeline.config.visualizer.show_id = ui_value
```

---

## 六、改进建议

### 6.1 短期改进 (1-2周)

#### 1. 添加单元测试覆盖

**目标**: 提高测试覆盖率到 90%+

**行动**:
- 为 `Visualizer` 添加单元测试
- 为 `FrameStateCache` 添加单元测试
- 为 `MetricsCollector` 添加单元测试
- 为 `VideoCanvas` 添加单元测试

#### 2. 优化日志输出

**目标**: 日志更加结构化和可读

**行动**:
- 使用结构化日志 (JSON格式)
- 添加请求ID追踪
- 优化日志级别使用

#### 3. 添加性能监控

**目标**: 实时监控系统性能

**行动**:
- 添加内存使用监控
- 添加GPU使用率监控
- 添加推理时间分布统计

### 6.2 中期改进 (1-2月)

#### 1. 重构 Pipeline 职责

**目标**: 明确职责边界，提高可维护性

**行动**:
- 将检测和跟踪分离到独立模块
- 使用依赖注入替代直接调用
- 添加接口抽象层

#### 2. 优化跳帧策略

**目标**: 提高跳帧时的视觉质量

**行动**:
- 实现卡尔曼滤波预测
- 添加轨迹插值算法
- 优化缓存策略

#### 3. 支持多摄像头

**目标**: 支持同时处理多个视频源

**行动**:
- 重构 Pipeline 支持多源
- 添加资源池管理
- 优化并发处理

### 6.3 长期改进 (3-6月)

#### 1. 引入服务层

**目标**: 解耦GUI和核心层

**行动**:
- 创建 Service 层封装 Pipeline
- 实现业务逻辑抽象
- 提高可测试性

#### 2. 支持分布式处理

**目标**: 支持大规模视频处理

**行动**:
- 添加任务队列
- 实现分布式推理
- 优化资源调度

#### 3. 添加Web界面

**目标**: 提供跨平台访问

**行动**:
- 使用 FastAPI 创建REST API
- 使用 WebSocket 实现实时推送
- 添加前端界面 (React/Vue)

---

## 七、总结

### 7.1 项目优势

1. **架构清晰**: 五层架构设计，职责明确
2. **模块化**: 各模块独立，易于扩展
3. **类型安全**: 使用 Pydantic 和 dataclass 保证类型安全
4. **配置灵活**: YAML配置文件，支持参数覆盖
5. **日志完善**: Loguru日志系统，支持轮转和保留
6. **测试覆盖**: 85个单元测试，覆盖核心功能
7. **GUI友好**: PySide6图形界面，用户体验好
8. **性能优化**: Worker线程、跳帧、缓存等优化

### 7.2 已修复问题

| 问题 | 状态 | 修复日期 | 严重程度 |
|------|------|---------|-----------|
| GUI主线程阻塞 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| 导出功能占位 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| show_progress配置未使用 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| config_changed信号不完整 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| BoundingBox无输入验证 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| CSV无原子写入 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| 参数覆盖使用truthy判断 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| GUI直接访问核心层私有成员 | ✅ 已修复 | 2026-04-14 | 🔴 高 |
| 跳帧时视觉不连续 | ✅ 已修复 | 2026-04-15 | 🔴 高 |
| FPS多源计算问题 | ✅ 已修复 | 2026-04-15 | 🔴 高 |
| 视频画面位置跳动 | ✅ 已修复 | 2026-04-15 | 🔴 高 |
| GUI显示开关不生效 | ✅ 已修复 | 2026-04-15 | 🔴 高 |
| device默认为cuda | ✅ 已修复 | 2026-04-15 | 🔴 高 |
| 测试覆盖不足 | ✅ 已修复 | 2026-04-14 | 🟡 中 |
| 内存泄漏 - Visualizer._color_map 无上限 | ⚠️ 未修复 | - | 🔴 高 |
| 线程安全 - GUI Worker数据共享无锁保护 | ⚠️ 未修复 | - | 🔴 高 |
| 资源泄漏 - Camera和Mock未正确清理 | ⚠️ 未修复 | - | 🔴 高 |
| 配置验证缺失 - 颜色值未验证 | ⚠️ 未修复 | - | 🟡 中 |
| 异常处理不足 - Worker异常可能静默失败 | ⚠️ 部分修复 | - | 🟡 中 |
| CSV导出原子性限制 - Windows下非真正原子 | ⚠️ 未修复 | - | 🟡 中 |

### 7.3 设计决策

1. **Pipeline直接调用YOLO.track()**: 性能优化设计，避免重复计算
2. **FrameStateCache**: 保持跳帧时视觉连续性
3. **MetricsCollector**: 统一性能指标计算，避免多源问题
4. **Worker线程**: 避免GUI主线程阻塞
5. **auto设备选择**: 自动选择最佳设备，提高兼容性

### 7.4 代码质量

- **类型安全**: ✅ 使用 Pydantic 和 dataclass
- **输入验证**: ✅ BoundingBox 添加验证
- **异常处理**: ✅ 完整的异常层次结构
- **日志记录**: ✅ Loguru日志系统
- **测试覆盖**: ✅ 7个测试文件，91个测试用例
- **文档完善**: ✅ README、AGENTS.md、HANDOFF文档

### 7.5 总体评价

这是一个**架构清晰、模块化良好、代码质量高**的项目。经过多轮技术债修复，主要问题已解决，系统稳定性和可维护性显著提升。

**核心优势**:
- 清晰的五层架构
- 完善的类型系统
- 灵活的配置管理
- 友好的GUI界面
- 良好的性能优化

**改进空间**:
- 测试覆盖率可进一步提高
- 可考虑引入服务层解耦
- 可支持分布式处理
- 可添加Web界面

---

## 八、遗漏模块详细分析

### 8.1 MetricsCollector (性能指标收集器)

#### 8.1.1 设计目的

**问题**: 之前存在多个地方计算FPS，导致显示不一致。

**解决方案**: 使用 `MetricsCollector` 统一性能指标计算，解决多源FPS计算不一致问题。

#### 8.1.2 数据结构

```python
MetricsCollector
    │
    ├─> _window_size: int  # 滑动窗口大小（默认30）
    ├─> _lock: threading.Lock  # 线程锁
    ├─> _frame_history: deque[FrameMetrics]  # 帧历史记录（滑动窗口）
    ├─> _total_frames: int  # 总帧数
    ├─> _start_time: Optional[float]  # 开始时间
    ├─> _last_update_time: Optional[float]  # 最后更新时间
    └─> _listeners: list[Callable]  # 回调函数列表

FrameMetrics
    │
    ├─> frame_id: int  # 帧编号
    ├─> timestamp: float  # 时间戳
    ├─> inference_time_ms: float  # 推理耗时（毫秒）
    └─> detection_count: int  # 检测数量

PerformanceSnapshot
    │
    ├─> fps: float  # 帧率
    ├─> inference_time_ms: float  # 平均推理时间
    ├─> detection_count: int  # 检测数量
    ├─> frame_count: int  # 总帧数
    └─> elapsed_time: float  # 总耗时
```

#### 8.1.3 FPS计算算法

```python
def _calculate_snapshot_unlocked(self) -> PerformanceSnapshot:
    """计算快照（内部使用，不持有锁）"""
    if not self._frame_history or self._start_time is None:
        return PerformanceSnapshot()
    
    # 计算FPS（基于滑动窗口）
    if len(self._frame_history) >= 2:
        time_span = self._frame_history[-1].timestamp - self._frame_history[0].timestamp
        if time_span > 0:
            fps = (len(self._frame_history) - 1) / time_span  # 帧数 / 时间跨度
        else:
            fps = 0.0
    else:
        fps = 0.0
    
    # 计算平均推理时间
    if self._frame_history:
        avg_inference = sum(m.inference_time_ms for m in self._frame_history) / len(self._frame_history)
    else:
        avg_inference = 0.0
    
    # 获取最新检测数
    detection_count = self._frame_history[-1].detection_count if self._frame_history else 0
    
    # 计算总耗时
    elapsed = self._last_update_time - self._start_time if self._last_update_time else 0
    
    return PerformanceSnapshot(
        fps=fps,
        inference_time_ms=avg_inference,
        detection_count=detection_count,
        frame_count=self._total_frames,
        elapsed_time=elapsed
    )
```

**FPS计算说明**:
- 使用滑动窗口（默认30帧）计算FPS
- FPS = (窗口内帧数 - 1) / 时间跨度
- 避免了单帧计算的不稳定性
- 提供了平滑的FPS显示

#### 8.1.4 线程安全

```python
def report_frame(self, frame_id: int, inference_time_ms: float = 0.0, detection_count: int = 0) -> None:
    """报告一帧的处理结果"""
    now = time.time()
    
    with self._lock:  # 加锁保护
        if self._start_time is None:
            self._start_time = now
            self._last_update_time = now
        
        metrics = FrameMetrics(
            frame_id=frame_id,
            timestamp=now,
            inference_time_ms=inference_time_ms,
            detection_count=detection_count
        )
        
        self._frame_history.append(metrics)  # 自动限制大小
        self._total_frames += 1
        self._last_update_time = now
        
        # 触发回调
        snapshot = self._calculate_snapshot_unlocked()
    
    # 在锁外触发回调，避免死锁
    for listener in self._listeners:
        try:
            listener(snapshot)
        except Exception:
            pass
```

**线程安全机制**:
- 使用 `threading.Lock` 保护共享数据
- 回调在锁外触发，避免死锁
- 滑动窗口自动限制大小，避免内存无限增长

### 8.2 Logger (日志系统)

#### 8.2.1 日志配置

```python
def setup_logger(
    level: str = "INFO",
    log_file: Optional[str] = None,
    rotation: str = "10 MB",
    retention: str = "7 days",
    format_string: Optional[str] = None,
) -> Logger:
    """配置系统日志器"""
    # 移除默认处理器
    logger.remove()
    
    # 默认日志格式
    if format_string is None:
        format_string = (
            "<green>{time:YYYY-MM-DD HH:mm:ss}</green> | "
            "<level>{level: <8}</level> | "
            "<cyan>{name}</cyan>:<cyan>{function}</cyan>:<cyan>{line}</cyan> | "
            "<level>{message}</level>"
        )
    
    # 添加控制台处理器
    logger.add(
        sys.stdout,
        level=level,
        format=format_string,
        colorize=True,  # 彩色输出
        enqueue=True,  # 异步写入
    )
    
    # 添加文件处理器（如果指定了日志文件）
    if log_file:
        # 确保日志目录存在
        log_path = Path(log_file)
        log_path.parent.mkdir(parents=True, exist_ok=True)
        
        # 文件格式（不带颜色）
        file_format = (
            "{time:YYYY-MM-DD HH:mm:ss} | "
            "{level: <8} | "
            "{name}:{function}:{line} | "
            "{message}"
        )
        
        logger.add(
            log_file,
            level=level,
            format=file_format,
            rotation=rotation,  # 日志轮转
            retention=retention,  # 日志保留
            compression="zip",  # 压缩旧日志
            enqueue=True,  # 异步写入
            encoding="utf-8",
        )
    
    return logger
```

#### 8.2.2 日志轮转机制

**轮转策略**:
- **大小轮转**: 当日志文件达到指定大小时（如10MB），自动创建新文件
- **时间轮转**: 支持按时间轮转（如"1 day"）
- **压缩**: 旧日志自动压缩为zip格式
- **保留**: 保留指定时间的日志（如"7 days"），超过的自动删除

**日志文件命名**:
```
logs/tracking.log          # 当前日志
logs/tracking.2024-04-15.log  # 轮转的日志
logs/tracking.2024-04-14.log.zip  # 压缩的旧日志
```

#### 8.2.3 模块级日志器

```python
def get_logger(name: Optional[str] = None) -> Logger:
    """获取日志器
    
    Args:
        name: 模块名称，为 None 返回默认日志器
    
    Returns:
        Logger 实例
    
    Example:
        >>> log = get_logger("detector")
        >>> log.info("Model loaded successfully")
    """
    if name:
        return logger.bind(name=name)  # 绑定模块名称
    return logger
```

**使用示例**:
```python
# 在各个模块中使用
from ..infra.logger import get_logger

logger = get_logger("detector")  # 绑定模块名称
logger.info("Model loaded successfully")
logger.debug("Processing frame 100")
```

### 8.3 Exceptions (异常处理)

#### 8.3.1 异常层次结构

```
TrackingSystemError (基类)
    │
    ├─> ModelLoadError      - 模型加载失败
    ├─> VideoLoadError      - 视频/摄像头加载失败
    ├─> ConfigurationError  - 配置错误
    ├─> InferenceError      - 推理过程错误
    └─> ExportError         - 导出错误
```

#### 8.3.2 异常传播路径

```python
# 1. 模型加载异常
try:
    self._model = YOLO(self.config.detector.model_path)
except Exception as e:
    raise ModelLoadError(
        f"Failed to load YOLOv11 model",
        model_path=self.config.detector.model_path,
        details=e,
    )

# 2. 视频加载异常
try:
    self._cap = cv2.VideoCapture(str(self.source))
    if not self._cap.isOpened():
        raise VideoLoadError(
            "Failed to open video source",
            source=self.source,
        )
except Exception as e:
    raise VideoLoadError(
        "Failed to open video source",
        source=self.source,
        details=e,
    )

# 3. 推理异常
try:
    results = self.model.track(frame.image, ...)
except Exception as e:
    raise InferenceError(
        f"Detection inference failed",
        frame_id=frame.frame_id,
        details=e,
    )

# 4. 导出异常
try:
    with open(temp_path, 'w', newline='', encoding='utf-8') as f:
        writer = csv.writer(f)
        writer.writerow(self._header)
except Exception as e:
    raise ExportError(
        f"Failed to write CSV",
        output_path=self._csv_path,
        details=e,
    )
```

#### 8.3.3 异常处理策略

| 异常类型 | 触发场景 | 处理策略 | 是否可恢复 |
|----------|----------|----------|-----------|
| ModelLoadError | 模型文件不存在或加载失败 | 记录错误，提示下载，退出 | ❌ 不可恢复 |
| VideoLoadError | 视频/摄像头无法打开 | 记录错误，退出 | ❌ 不可恢复 |
| ConfigurationError | 配置文件格式错误 | 使用默认配置 + 警告日志 | ✅ 可恢复 |
| InferenceError | 单帧推理失败 | 跳过该帧，继续处理 | ✅ 可恢复 |
| ExportError | CSV 写入失败 | 记录错误，不中断处理 | ✅ 可恢复 |

### 8.4 PersonDetector (检测器)

#### 8.4.1 类职责

虽然 `TrackingPipeline` 直接调用 `YOLO.track()`，但 `PersonDetector` 类仍然存在，用于：
- 封装 YOLOv11 检测模型
- 提供独立的检测接口
- 支持仅检测模式（不跟踪）

#### 8.4.2 核心方法

```python
class PersonDetector:
    """人物检测器"""
    
    def __init__(self, config: DetectorConfig) -> None:
        """初始化人物检测器"""
        self.config = config
        self._model = None
        self._warmup_done = False
        self._load_model()  # 加载模型
    
    def detect(self, image: NDArray[np.uint8], frame_id: Optional[int] = None) -> list[Detection]:
        """检测图像中的人物
        
        Args:
            image: BGR 格式图像数组 (H, W, C)
            frame_id: 帧编号（用于错误报告）
        
        Returns:
            Detection 列表（仅包含 person 类别）
        """
        if self._model is None:
            raise ModelLoadError("Model not loaded")
        
        try:
            # 运行推理
            results = self._model(
                image,
                conf=self.config.confidence_threshold,
                iou=self.config.iou_threshold,
                classes=self.config.classes,  # 只检测 person
                imgsz=self.config.imgsz,
                verbose=False,
            )
            
            # 转换结果
            detections = self._convert_results(results)
            
            return detections
        
        except Exception as e:
            raise InferenceError(
                f"Detection inference failed",
                frame_id=frame_id,
                details=e,
            )
    
    def _convert_results(self, results) -> list[Detection]:
        """转换 YOLO 结果为 Detection 格式"""
        detections = []
        
        if not results or len(results) == 0:
            return detections
        
        result = results[0]
        
        if result.boxes is None or len(result.boxes) == 0:
            return detections
        
        boxes = result.boxes
        
        # 提取边界框、置信度和类别
        xyxy = boxes.xyxy.cpu().numpy()
        confidences = boxes.conf.cpu().numpy()
        class_ids = boxes.cls.cpu().numpy().astype(int)
        
        # 转换为 Detection 对象
        for i in range(len(xyxy)):
            x1, y1, x2, y2 = xyxy[i]
            confidence = float(confidences[i])
            class_id = int(class_ids[i])
            
            bbox = BoundingBox.from_xyxy(
                x1=float(x1),
                y1=float(y1),
                x2=float(x2),
                y2=float(y2),
                confidence=confidence,
            )
            
            detection = Detection(
                bbox=bbox,
                class_id=class_id,
                class_name="person",  # 只处理 person
            )
            
            detections.append(detection)
        
        return detections
```

#### 8.4.3 使用场景

```python
# 场景1: 仅检测（不跟踪）
detector = PersonDetector(config)
detections = detector.detect(frame)
for det in detections:
    print(f"Person detected: confidence={det.confidence:.2f}")

# 场景2: 检测+跟踪（使用Pipeline）
pipeline = TrackingPipeline(config)
processed_frame, tracked_objects = pipeline.process_frame(frame)
```

### 8.5 PersonTracker (跟踪器)

#### 8.5.1 类职责

`PersonTracker` 类封装 ByteTrack 跟踪算法，提供：
- 跨帧目标关联
- track_id 维护
- 结果转换为标准 TrackedObject 格式

#### 8.5.2 核心方法

```python
class PersonTracker:
    """人物跟踪器"""
    
    def __init__(self, config: TrackerConfig) -> None:
        """初始化人物跟踪器"""
        self.config = config
        self._tracker_config = None
        self._frame_count = 0
        self._setup_tracker()  # 创建跟踪器配置
    
    def _setup_tracker(self) -> None:
        """设置跟踪器配置"""
        # 创建 ByteTrack 配置字典
        self._tracker_config = {
            "tracker_type": self.config.tracker_type,
            "track_buffer": self.config.track_buffer,
            "match_thresh": self.config.match_thresh,
            "track_thresh": self.config.track_thresh,
            "new_track_thresh": self.config.new_track_thresh,
        }
    
    def update(self, detections: list[Detection], frame: NDArray[np.uint8], frame_id: int = 0, timestamp: float = 0.0) -> list[TrackedObject]:
        """更新跟踪状态
        
        Args:
            detections: 当前帧检测结果
            frame: 当前帧图像（BGR 格式）
            frame_id: 帧编号
            timestamp: 时间戳
        
        Returns:
            TrackedObject 列表
        """
        # 将检测结果转换为带 track_id 的跟踪对象
        tracked_objects = []
        
        for detection in detections:
            # 如果检测已经有 track_id，创建 TrackedObject
            if detection.track_id is not None:
                tracked_obj = TrackedObject(
                    track_id=detection.track_id,
                    detection=detection,
                    frame_id=frame_id,
                    timestamp=timestamp,
                )
                tracked_objects.append(tracked_obj)
        
        self._frame_count += 1
        return tracked_objects
```

#### 8.5.3 YOLOTrackerWrapper

```python
class YOLOTrackerWrapper:
    """YOLO 跟踪器封装
    
    直接使用 Ultralytics YOLO 的 track() 方法进行检测+跟踪一体化。
    """
    
    def track(self, frame: NDArray[np.uint8], frame_id: int = 0, timestamp: float = 0.0, confidence: float = 0.5, classes: list[int] = None) -> list[TrackedObject]:
        """检测并跟踪
        
        一体化检测+跟踪，返回带 track_id 的跟踪对象。
        """
        if classes is None:
            classes = [0]  # 默认只检测 person
        
        try:
            # 使用 YOLO 的 track 方法
            results = self.model.track(
                frame,
                conf=confidence,
                classes=classes,
                tracker=f"{self.tracker_config.tracker_type}.yaml",
                persist=True,
                verbose=False,
            )
            
            # 转换结果
            tracked_objects = self._convert_results(results, frame_id, timestamp)
            
            self._frame_count += 1
            return tracked_objects
        
        except Exception as e:
            raise InferenceError(
                f"Tracking inference failed",
                frame_id=frame_id,
                details=e,
            )
```

### 8.6 VideoLoader (视频加载器)

#### 8.6.1 支持的视频源

| 视频源类型 | 示例 | 说明 |
|-----------|------|------|
| 视频文件 | `"video.mp4"`, `"C:/path/to/video.avi"` | 支持MP4、AVI、MOV等格式 |
| 摄像头 | `0`, `1`, `2` | 摄像头索引，0为默认摄像头 |
| RTSP流 | `"rtsp://192.168.1.100:554/stream"` | 网络摄像头流 |

#### 8.6.2 核心方法

```python
class VideoLoader:
    """视频/摄像头加载器"""
    
    def __init__(self, source: Union[str, int, Path], start_frame: int = 0) -> None:
        """初始化视频加载器
        
        Args:
            source: 视频源（文件路径或摄像头索引）
            start_frame: 起始帧号（仅视频文件有效）
        """
        self.source = source
        self.start_frame = start_frame
        self._frame_id = 0
        self._cap: Optional[cv2.VideoCapture] = None
        self._is_camera = False
        
        # 初始化视频捕获
        self._init_capture()
    
    def _init_capture(self) -> None:
        """初始化视频捕获对象"""
        # 创建 VideoCapture 对象
        if isinstance(self.source, (str, Path)):
            self._cap = cv2.VideoCapture(str(self.source))
            self._is_camera = False
        else:
            self._cap = cv2.VideoCapture(self.source)
            self._is_camera = True
        
        # 检查是否成功打开
        if not self._cap.isOpened():
            raise VideoLoadError(
                "Failed to open video source",
                source=self.source,
            )
        
        # 获取视频属性
        self._fps = self._cap.get(cv2.CAP_PROP_FPS)
        self._frame_count = int(self._cap.get(cv2.CAP_PROP_FRAME_COUNT))
        self._width = int(self._cap.get(cv2.CAP_PROP_FRAME_WIDTH))
        self._height = int(self._cap.get(cv2.CAP_PROP_FRAME_HEIGHT))
        
        # 跳转到起始帧
        if self.start_frame > 0 and not self._is_camera:
            self._cap.set(cv2.CAP_PROP_POS_FRAMES, self.start_frame)
            self._frame_id = self.start_frame
    
    def read_frame(self) -> Optional[Frame]:
        """读取单帧
        
        Returns:
            Frame 对象，如果到达视频末尾或读取失败返回 None
        """
        if self._cap is None or not self._cap.isOpened():
            return None
        
        # 读取帧
        ret, image = self._cap.read()
        
        if not ret:
            return None
        
        # 计算时间戳
        timestamp = self._frame_id / self.fps if self.fps > 0 else 0.0
        
        # 创建 Frame 对象
        frame = Frame(
            frame_id=self._frame_id,
            timestamp=timestamp,
            image=image,
        )
        
        self._frame_id += 1
        
        return frame
    
    def seek(self, frame_id: int) -> bool:
        """跳转到指定帧（仅视频文件有效）
        
        Args:
            frame_id: 目标帧 ID
        
        Returns:
            是否跳转成功
        
        Note:
            摄像头不支持跳转。
        """
        if self._is_camera or self._cap is None:
            return False
        
        if frame_id < 0 or frame_id >= self._frame_count:
            return False
        
        self._cap.set(cv2.CAP_PROP_POS_FRAMES, frame_id)
        self._frame_id = frame_id
        return True
    
    def release(self) -> None:
        """释放资源
        
        关闭视频捕获对象，释放摄像头。
        """
        if self._cap is not None:
            self._cap.release()
            self._cap = None
            logger.debug(f"VideoLoader released: source={self.source}")
```

#### 8.6.3 错误处理

```python
# 视频源打开失败
try:
    loader = VideoLoader("nonexistent.mp4")
except VideoLoadError as e:
    print(f"Failed to load video: {e}")
    print(f"Source: {e.source}")
    print(f"Details: {e.details}")

# 摄像头被占用
try:
    loader = VideoLoader(0)  # 摄像头被其他程序占用
except VideoLoadError as e:
    print(f"Failed to open camera: {e}")
```

### 8.7 TrackingController (GUI控制器)

#### 8.7.1 职责

`TrackingController` 是GUI层的控制器，负责：
- 协调 Pipeline、Workers、Config 之间的交互
- 处理业务逻辑（开始/停止/暂停/导出）
- 状态管理（运行状态、视频信息）
- 不直接操作UI，通过信号与UI通信

#### 8.7.2 状态机

```python
class AppState(Enum):
    """应用状态"""
    IDLE = auto()      # 就绪
    RUNNING = auto()   # 运行中
    PAUSED = auto()    # 已暂停
    ERROR = auto()     # 错误状态

# 状态转换
# IDLE -> RUNNING (开始)
# RUNNING -> PAUSED (暂停)
# PAUSED -> RUNNING (继续)
# RUNNING/PAUSED -> IDLE (停止)
# 任何状态 -> ERROR (错误)
```

#### 8.7.3 核心方法

```python
class TrackingController(QObject):
    """跟踪控制器"""
    
    # 信号定义
    state_changed = Signal(object)  # AppState
    frame_ready = Signal(object)  # NDArray[np.uint8]
    metrics_updated = Signal(dict)
    error_occurred = Signal(str)
    progress_updated = Signal(int, int)  # current, total
    
    def __init__(self, parent: Optional[QObject] = None) -> None:
        """初始化控制器"""
        super().__init__(parent)
        self._state = AppState.IDLE
        self._config: Optional[Config] = None
        self._pipeline: Optional[TrackingPipeline] = None
        self._video_info = VideoInfo()
        
        # Workers（在start时初始化）
        self._capture_worker = None
        self._inference_worker = None
        
        # 当前源
        self._current_source: Optional[str] = None
    
    def initialize(self, config: Optional[Config] = None) -> bool:
        """初始化控制器
        
        Args:
            config: 配置对象，None则使用默认配置
        
        Returns:
            是否初始化成功
        """
        try:
            self._config = config or load_config()
            self._set_state(AppState.IDLE)
            return True
        except Exception as e:
            self.error_occurred.emit(f"初始化失败: {str(e)}")
            return False
    
    def start(self) -> bool:
        """开始处理
        
        Returns:
            是否成功启动
        """
        if self._state == AppState.RUNNING:
            return True
        
        if not self._current_source:
            self.error_occurred.emit("未选择视频源")
            return False
        
        try:
            self._set_state(AppState.RUNNING)
            return True
        except Exception as e:
            self.error_occurred.emit(f"启动失败: {str(e)}")
            self._set_state(AppState.ERROR)
            return False
    
    def pause(self) -> None:
        """暂停处理"""
        if self._state == AppState.RUNNING:
            self._set_state(AppState.PAUSED)
    
    def stop(self) -> None:
        """停止处理"""
        self._set_state(AppState.IDLE)
        self._cleanup()
    
    def update_config(self, key: str, value: Any) -> None:
        """更新配置项（热更新）
        
        Args:
            key: 配置项名称
            value: 新值
        """
        if self._config is None:
            return
        
        # 映射UI配置到Pipeline配置
        config_map = {
            'confidence_threshold': ('detector', 'confidence_threshold'),
            'iou_threshold': ('detector', 'iou_threshold'),
            'device': ('detector', 'device'),
            'show_bbox': ('visualizer', 'show_bbox'),
            'show_trajectory': ('visualizer', 'show_trajectory'),
            'show_id': ('visualizer', 'show_id'),
            'show_center_point': ('visualizer', 'show_center_point'),
            'show_confidence': ('visualizer', 'show_confidence'),
        }
        
        if key in config_map:
            section, attr = config_map[key]
            setattr(getattr(self._config, section), attr, value)
            
            # 如果Pipeline已初始化，同步更新
            if self._pipeline and hasattr(self._pipeline, 'visualizer'):
                viz_config = self._pipeline.visualizer.config
                if hasattr(viz_config, attr):
                    setattr(viz_config, attr, value)
    
    def _set_state(self, new_state: AppState) -> None:
        """设置新状态
        
        Args:
            new_state: 新状态
        """
        old_state = self._state
        self._state = new_state
        if old_state != new_state:
            self.state_changed.emit(new_state)
    
    def _cleanup(self) -> None:
        """清理资源"""
        self._capture_worker = None
        self._inference_worker = None
```

### 8.8 GUI组件

#### 8.8.1 TrajectoryList (轨迹列表)

```python
class TrajectoryList(QWidget):
    """轨迹列表组件
    
    显示选中目标的轨迹历史记录，支持 ID 选择和数据更新。
    """
    
    track_id_selected = Signal(int)
    
    def __init__(self, parent: Optional[QWidget] = None) -> None:
        """初始化轨迹列表组件"""
        super().__init__(parent)
        
        # 轨迹数据存储
        self._trajectories: dict[int, list] = {}  # {track_id: [(x, y, t), ...]}
        self._current_track_id: Optional[int] = None
        
        self._init_ui()
    
    def update_trajectories(self, trajectories: dict[int, list]) -> None:
        """更新轨迹数据
        
        Args:
            trajectories: 轨迹数据字典 {track_id: [(x, y, t), ...]}
        """
        self._trajectories = trajectories
        
        # 更新 ID 下拉框
        current_ids = set(self._trajectories.keys())
        old_ids = {self._id_combo.itemData(i) for i in range(self._id_combo.count())}
        
        # 添加新 ID
        for tid in current_ids - old_ids:
            self._id_combo.addItem(f"ID: {tid}", tid)
        
        # 移除不存在的 ID
        for i in range(self._id_combo.count() - 1, -1, -1):
            if self._id_combo.itemData(i) not in current_ids:
                self._id_combo.removeItem(i)
        
        # 更新当前显示
        self._update_display()
    
    def _update_display(self) -> None:
        """更新显示内容"""
        self._point_list.clear()
        
        if self._current_track_id is None or self._current_track_id not in self._trajectories:
            self._info_label.setText("点数: 0 | 持续: 0.0s")
            return
        
        points = self._trajectories[self._current_track_id]
        
        # 更新信息
        if points:
            duration = points[-1][2] - points[0][2] if len(points) > 1 else 0.0
            self._info_label.setText(f"点数: {len(points)} | 持续: {duration:.1f}s")
        else:
            self._info_label.setText("点数: 0 | 持续: 0.0s")
        
        # 显示最近的轨迹点 (最多50个)
        recent_points = points[-50:] if len(points) > 50 else points
        
        for i, (x, y, t) in enumerate(reversed(recent_points)):
            item = QListWidgetItem(f"({int(x)}, {int(y)}) @ {t:.3f}s")
            self._point_list.addItem(item)
```

#### 8.8.2 LogPanel (日志面板)

```python
class LogPanel(QWidget):
    """日志面板组件
    
    显示系统运行日志，支持不同日志级别的颜色区分。
    """
    
    clear_requested = Signal()
    
    # 日志级别颜色配置
    LEVEL_COLORS = {
        "DEBUG": QColor("#808080"),
        "INFO": QColor("#D4D4D4"),
        "WARNING": QColor("#DC8C6A"),
        "ERROR": QColor("#CE9178"),
        "CRITICAL": QColor("#FF0000"),
    }
    
    def __init__(self, max_entries: int = 500, parent: Optional[QWidget] = None) -> None:
        """初始化日志面板
        
        Args:
            max_entries: 最大日志条目数
            parent: 父组件
        """
        super().__init__(parent)
        
        self._max_entries = max_entries
        self._entries: deque = deque(maxlen=max_entries)
        
        self._init_ui()
    
    def log(self, message: str, level: str = "INFO") -> None:
        """添加日志条目
        
        Args:
            message: 日志消息
            level: 日志级别 (DEBUG/INFO/WARNING/ERROR/CRITICAL)
        """
        timestamp = datetime.now().strftime("%H:%M:%S")
        entry = f"[{timestamp}] [{level}] {message}"
        
        self._entries.append((entry, level))
        self._append_log(entry, level)
        self._update_count()
    
    def _append_log(self, entry: str, level: str) -> None:
        """追加日志到文本区域
        
        Args:
            entry: 日志条目
            level: 日志级别
        """
        cursor = self._log_text.textCursor()
        cursor.movePosition(QTextCursor.MoveOperation.End)
        
        # 设置格式
        format = QTextCharFormat()
        color = self.LEVEL_COLORS.get(level, QColor("#D4D4D4"))
        format.setForeground(color)
        
        cursor.insertText(entry + "\n", format)
        
        # 滚动到底部
        scrollbar = self._log_text.verticalScrollBar()
        scrollbar.setValue(scrollbar.maximum())
```

### 8.9 配置文件详解

#### 8.9.1 default.yaml 结构

```yaml
# 检测器配置
detector:
  model_path: "yolo11n.pt"           # 模型路径
  confidence_threshold: 0.5           # 检测置信度阈值 [0.0, 1.0]
  iou_threshold: 0.45                 # NMS IOU 阈值 [0.0, 1.0]
  device: "cuda"                     # 推理设备 (cuda/cpu/mps/auto)
  classes: [0]                        # 检测类别 (COCO: person=0)
  imgsz: 640                          # 推理图像尺寸

# 跟踪器配置
tracker:
  tracker_type: "bytetrack"          # 跟踪器类型 (bytetrack/botsort)
  track_buffer: 30                    # 轨迹缓冲帧数
  match_thresh: 0.8                   # 匹配阈值 [0.0, 1.0]
  track_thresh: 0.5                   # 跟踪置信度阈值 [0.0, 1.0]
  new_track_thresh: 0.6               # 新建轨迹阈值 [0.0, 1.0]

# 可视化配置
visualizer:
  box_color: [0, 255, 0]              # 边界框颜色 (BGR)
  text_color: [255, 255, 255]         # 文本颜色 (BGR)
  trajectory_color: [255, 0, 0]       # 轨迹颜色 (BGR)
  line_thickness: 2                    # 线条宽度 [1, 10]
  trajectory_length: 50               # 显示的轨迹点数量
  show_center_point: true              # 是否显示中心点
  show_confidence: true               # 是否显示置信度
  font_scale: 0.6                     # 字体缩放比例 [0.1, 2.0]
  show_bbox: true                     # 是否显示边界框 (GUI开关)
  show_trajectory: true               # 是否显示轨迹 (GUI开关)
  show_id: true                       # 是否显示ID标签 (GUI开关)

# 处理管道配置
pipeline:
  show_progress: true                  # 是否显示进度条
  save_output: true                   # 是否保存输出视频
  output_fps: null                    # 输出视频帧率 (null 使用原视频帧率)
  skip_frames: 0                       # 跳帧处理数量 (0 表示不跳帧)
  warmup: true                         # 是否进行模型预热

# 日志配置
logging:
  level: "INFO"                       # 日志级别 (DEBUG/INFO/WARNING/ERROR/CRITICAL)
  format: "..."                        # 日志格式
  file: "logs/tracking.log"          # 日志文件路径
  rotation: "10 MB"                    # 日志轮转大小
  retention: "7 days"                  # 日志保留时间
```

#### 8.9.2 bytetrack.yaml 结构

```yaml
# ByteTrack 跟踪器配置文件
tracker_type: bytetrack

# 核心参数
track_buffer: 30                      # 轨迹缓冲帧数
track_thresh: 0.5                     # 跟踪阈值
new_track_thresh: 0.6                 # 新轨迹阈值
match_thresh: 0.8                     # 匹配阈值

# 高级参数（可选）
# min_box_area: 10                     # 最小检测框面积
# max_box_area_ratio: 0.8              # 最大检测框面积比例
```

#### 8.9.3 ByteTrack参数说明

| 参数 | 说明 | 推荐值 | 影响 |
|------|------|--------|------|
| `track_buffer` | 目标消失后保留的帧数 | 30 | 值越大，遮挡恢复能力越强，但误关联风险也越高 |
| `track_thresh` | 高置信度检测阈值 | 0.5 | 高于此阈值的检测结果直接用于轨迹关联 |
| `new_track_thresh` | 新建轨迹阈值 | 0.6 | 高于此阈值的检测结果可以创建新的轨迹 |
| `match_thresh` | 轨迹与检测框的IOU阈值 | 0.8 | 值越大，匹配条件越严格 |

### 8.10 YOLO.track() 返回结果详解

#### 8.10.1 返回结果结构

```python
results = model.track(
    frame,
    conf=0.5,
    iou=0.45,
    classes=[0],
    tracker="bytetrack.yaml",
    persist=True,
    verbose=False,
)

# results 是一个列表，每个元素对应一个图像的检测结果
# 对于单帧处理，results[0] 包含所有检测框

result = results[0]

# result.boxes 包含所有检测框
# result.boxes.xyxy: 边界框坐标 (N, 4) - [[x1, y1, x2, y2], ...]
# result.boxes.conf: 置信度 (N,) - [0.95, 0.87, ...]
# result.boxes.cls: 类别ID (N,) - [0, 0, ...]
# result.boxes.id: 跟踪ID (N,) - [1, 2, 3, ...] (仅跟踪模式)
```

#### 8.10.2 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `xyxy` | NDArray | 边界框坐标 (N, 4) | `[[100, 100, 200, 200], [300, 150, 400, 250]]` |
| `conf` | NDArray | 置信度 (N,) | `[0.95, 0.87, 0.92]` |
| `cls` | NDArray | 类别ID (N,) | `[0, 0, 0]` (person=0) |
| `id` | NDArray | 跟踪ID (N,) | `[1, 2, 3]` (仅跟踪模式) |

#### 8.10.3 转换为TrackedObject

```python
def _convert_results(self, results, frame_id: int, timestamp: float) -> list[TrackedObject]:
    """转换 YOLO 跟踪结果"""
    tracked_objects = []
    
    if not results or len(results) == 0:
        return tracked_objects
    
    result = results[0]
    
    if result.boxes is None or len(result.boxes) == 0:
        return tracked_objects
    
    boxes = result.boxes
    
    # 检查是否有 track_id（跟踪模式）
    has_tracking = boxes.id is not None
    
    xyxy = boxes.xyxy.cpu().numpy()
    confidences = boxes.conf.cpu().numpy()
    class_ids = boxes.cls.cpu().numpy().astype(int)
    
    if has_tracking:
        track_ids = boxes.id.cpu().numpy().astype(int)
    else:
        # 仅检测模式，使用临时 ID
        track_ids = list(range(len(xyxy)))
    
    for i in range(len(xyxy)):
        x1, y1, x2, y2 = xyxy[i]
        confidence = float(confidences[i])
        class_id = int(class_ids[i])
        track_id = int(track_ids[i])
        
        bbox = BoundingBox.from_xyxy(
            x1=float(x1),
            y1=float(y1),
            x2=float(x2),
            y2=float(y2),
            confidence=confidence,
        )
        
        detection = Detection(
            bbox=bbox,
            class_id=class_id,
            class_name="person",
            track_id=track_id,
        )
        
        tracked_obj = TrackedObject(
            track_id=track_id,
            detection=detection,
            frame_id=frame_id,
            timestamp=timestamp,
        )
        
        tracked_objects.append(tracked_obj)
    
    return tracked_objects
```

### 8.11 skip_frames 逻辑详解

#### 8.11.1 跳帧公式

```python
# 跳帧逻辑：每 (skip_frames + 1) 帧处理一次
should_process = (
    skip_frames == 0 or
    frame_counter % (skip_frames + 1) == 1 or
    frame_counter == 1  # 第一帧总是处理
)
```

**数学公式**:
```
处理帧号 = {1, 1 + (skip_frames + 1), 1 + 2*(skip_frames + 1), ...}

示例：
- skip_frames = 0: 处理所有帧 {1, 2, 3, 4, 5, ...}
- skip_frames = 1: 处理 {1, 3, 5, 7, 9, ...} (每2帧处理1帧)
- skip_frames = 2: 处理 {1, 4, 7, 10, 13, ...} (每3帧处理1帧)
- skip_frames = 4: 处理 {1, 6, 11, 16, 21, ...} (每5帧处理1帧)
```

#### 8.11.2 跳帧对性能的影响

| skip_frames | 处理帧率 | 性能提升 | 视觉质量 |
|-------------|---------|---------|---------|
| 0 | 100% | 1x | 最佳 |
| 1 | 50% | 2x | 良好 |
| 2 | 33% | 3x | 可接受 |
| 4 | 20% | 5x | 较差 |
| 9 | 10% | 10x | 差 |

**建议**:
- 实时处理: `skip_frames = 0-2`
- 离线处理: `skip_frames = 0-4`
- 性能优先: `skip_frames = 2-4`

### 8.12 视频支持格式

#### 8.12.1 支持的视频格式

| 格式 | 扩展名 | 说明 |
|------|--------|------|
| MP4 | `.mp4` | 最常用的视频格式 |
| AVI | `.avi` | 传统视频格式 |
| MOV | `.mov` | Apple QuickTime格式 |
| MKV | `.mkv` | Matroska格式 |
| FLV | `.flv` | Flash视频格式 |
| WMV | `.wmv` | Windows Media格式 |

#### 8.12.2 支持的编码器

| 编码器 | 扩展名 | 说明 |
|--------|--------|------|
| mp4v | `.mp4` | MPEG-4编码 |
| XVID | `.avi` | XviD编码 |
| H264 | `.mp4`, `.avi` | H.264编码 |
| MJPG | `.avi` | Motion JPEG |

#### 8.12.3 摄像头支持

| 摄像头类型 | 索引 | 说明 |
|----------|------|------|
| USB摄像头 | `0` | 默认USB摄像头 |
| 网络摄像头 | `rtsp://...` | RTSP流 |
| IP摄像头 | `http://...` | HTTP流 |

### 8.13 CSVExporter (CSV导出器)

#### 8.13.1 类职责

`CSVExporter` 类负责将检测跟踪结果导出为CSV文件，支持：
- 结构化日志写入
- 批量导出
- 自动文件管理
- 原子写入（通过临时文件+重命名）

#### 8.13.2 核心方法

```python
class CSVExporter:
    """CSV日志导出器"""
    
    # CSV 表头
    HEADER = [
        "track_id",
        "frame_id",
        "timestamp",
        "x",
        "y",
        "w",
        "h",
        "confidence",
        "class_name",
    ]
    
    def __init__(self, output_path: str | Path, mode: str = "w", atomic: bool = True) -> None:
        """初始化CSV导出器
        
        Args:
            output_path: 输出文件路径
            mode: 写入模式 ('w'=覆盖, 'a'=追加)
            atomic: 是否使用原子写入（默认True）
        """
        self.output_path = Path(output_path)
        self.mode = mode
        self._atomic = atomic and mode == "w"
        self._file = None
        self._writer = None
        self._row_count = 0
        self._temp_path: Optional[Path] = None
        
        # 创建文件（如果使用原子写入，先写入临时文件）
        self._create_file()
    
    def write_row(self, tracked_obj: TrackedObject) -> None:
        """写入单条跟踪记录"""
        if self._writer is None:
            raise ExportError("CSV file not open")
        
        try:
            bbox = tracked_obj.bbox
            row = [
                tracked_obj.track_id,
                tracked_obj.frame_id,
                f"{tracked_obj.timestamp:.3f}",
                f"{bbox.x:.2f}",
                f"{bbox.y:.2f}",
                f"{bbox.w:.2f}",
                f"{bbox.h:.2f}",
                f"{bbox.confidence:.4f}",
                tracked_obj.detection.class_name,
            ]
            
            self.writer.writerow(row)
            self._row_count += 1
        
        except Exception as e:
            raise ExportError(
                f"Failed to write CSV row",
                output_path=str(self.output_path),
                details=e,
            )
    
    def write_batch(self, tracked_objects: List[TrackedObject]) -> None:
        """批量写入跟踪记录"""
        for obj in tracked_objects:
            self.write_row(obj)
    
    def close(self) -> None:
        """关闭文件
        
        如果使用原子写入模式，会将临时文件重命名为目标文件。
        在 Windows 上，文件必须先完全关闭才能重命名。
        """
        temp_path_to_rename = None
        
        if self._file is not None:
            self._file.flush()
            try:
                os.fsync(self._file.fileno())  # 确保数据写入磁盘
            except (OSError, ValueError):
                pass  # 某些情况下 fsync 可能失败
            
            # 获取临时文件路径（如果启用了原子写入）
            if self._atomic:
                temp_path_to_rename = self._temp_path
            
            self._file.close()
            self._file = None
            self._writer = None
        
        # 原子写入：临时文件重命名为目标文件
        # 必须在文件关闭后进行重命名（Windows 要求）
        if temp_path_to_rename is not None and temp_path_to_rename.exists():
            try:
                # 如果目标文件已存在，先删除（Windows 不支持覆盖重命名）
                if self.output_path.exists():
                    self.output_path.unlink()
                # 原子重命名
                os.rename(temp_path_to_rename, self.output_path)
                logger.debug(f"Atomic rename: {temp_path_to_rename} -> {self.output_path}")
            except OSError as e:
                # 清理临时文件
                if temp_path_to_rename.exists():
                    try:
                        temp_path_to_rename.unlink()
                    except OSError:
                        pass
                raise ExportError(
                    f"Failed to rename temp file to output",
                    output_path=str(self.output_path),
                    details=e,
                )
        
        self._temp_path = None
        
        logger.info(
            f"CSVExporter closed: {self._row_count} rows written to {self.output_path}"
        )
```

#### 8.13.3 原子写入机制

```python
def _create_file(self) -> None:
    """创建CSV文件并写入表头"""
    try:
        if self._atomic:
            # 原子写入：使用临时文件
            fd, temp_path = tempfile.mkstemp(
                suffix=".csv.tmp",
                prefix="tracking_",
                dir=str(self.output_path.parent),
            )
            os.close(fd)  # 关闭文件描述符，我们只需要路径
            self._temp_path = Path(temp_path)
            self._file = open(
                self._temp_path,
                "w",
                newline="",
                encoding="utf-8",
            )
        else:
            # 普通写入
            self._file = open(
                self.output_path,
                self.mode,
                newline="",
                encoding="utf-8",
            )
        
        self.writer = csv.writer(self._file)
        
        # 如果是覆盖模式或文件为空，写入表头
        if self.mode == "w":
            self.write_header()
```

**原子写入流程**:
1. 创建临时文件 `tracking_xxxxx.csv.tmp`
2. 写入表头和数据到临时文件
3. 关闭文件
4. 重命名临时文件为目标文件
5. 如果目标文件已存在，先删除（Windows要求）

**跨平台兼容性**:
- **Unix-like系统**: `os.replace()` 是原子的
- **Windows**: 需要先关闭文件再重命名

### 8.14 GUI启动流程 (app.py + run_gui.py)

#### 8.14.1 app.py - GUI应用入口

```python
def create_application(argv: Optional[list] = None) -> QApplication:
    """创建 QApplication 实例
    
    Args:
        argv: 命令行参数，默认使用 sys.argv
        
    Returns:
        配置好的 QApplication 实例
    """
    if argv is None:
        argv = sys.argv
    
    # 创建应用
    app = QApplication(argv)
    
    # 设置应用属性
    app.setApplicationName("Person Tracking System")
    app.setApplicationVersion("0.1.0")
    app.setOrganizationName("PersonTracking")
    app.setOrganizationDomain("persontracking.local")
    
    # 设置全局字体
    font = QFont("Microsoft YaHei", 10)
    app.setFont(font)
    
    # 加载样式表
    style = get_stylesheet()
    if style:
        app.setStyleSheet(style)
    
    return app


def create_main_window() -> "MainWindow":
    """创建主窗口
    
    Returns:
        配置好的 MainWindow 实例
    """
    # 延迟导入避免循环依赖
    from .main_window import MainWindow
    
    window = MainWindow()
    return window


def run_gui(argv: Optional[list] = None) -> int:
    """运行 GUI 应用
    
    Args:
        argv: 命令行参数
        
    Returns:
        应用退出码
    """
    # 创建应用
    app = create_application(argv)
    
    # 创建主窗口
    window = create_main_window()
    
    # 显示窗口
    window.show()
    
    # 运行事件循环
    return app.exec()
```

#### 8.14.2 run_gui.py - GUI启动脚本

```python
def main() -> int:
    """主入口函数"""
    try:
        # 标准导入方式 - 从 src.gui.app 导入 run_gui
        from src.gui.app import run_gui
        return run_gui()
    except ImportError as e:
        print(f"导入错误: {e}", file=sys.stderr)
        print("\n提示: 请确保从项目根目录运行此脚本", file=sys.stderr)
        print("  cd person_tracking", file=sys.stderr)
        print("  python run_gui.py", file=sys.stderr)
        return 1
    except Exception as e:
        print(f"启动错误: {e}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    sys.exit(main())
```

#### 8.14.3 启动流程

```
run_gui.py
    │
    ├─> main()
    │   │
    │   ├─> from src.gui.app import run_gui
    │   │
    │   └─> run_gui()
    │       │
    │       ├─> create_application()
    │       │   │
    │       ├─> create_main_window()
    │       │   │
    │       ├─> window.show()
    │       │   │
    │       └─> app.exec()
    │
    └─> sys.exit(exit_code)
```

**设计特点**:
- **标准导入**: 使用标准Python包导入，避免 `sys.path.insert` 绕过
- **错误处理**: 完善的导入错误和启动错误处理
- **样式加载**: 自动加载深色主题样式
- **字体配置**: 统一的字体设置

### 8.15 GUI常量配置 (constants.py)

#### 8.15.1 刷新节流参数

```python
# 目标表格刷新间隔 (每 N 帧刷新一次)
TARGET_TABLE_REFRESH_INTERVAL = 5  # 约每 5 帧刷新一次表格

# 轨迹列表刷新间隔 (每 N 帧刷新一次)
TRAJECTORY_LIST_REFRESH_INTERVAL = 10  # 线每 10 帧刷新一次轨迹

# 日志面板刷新间隔 (每 N 帧刷新一次)
LOG_PANEL_REFRESH_INTERVAL = 30  # 线每 30 帧刷新一次日志

# 状态栏 FPS 更新间隔 (秒)
STATUS_BAR_FPS_UPDATE_INTERVAL = 0.5  # 0.5 秒更新一次 FPS 显示
```

#### 8.15.2 视频显示参数

```python
# 实时预览模式: 是否使用轻量显示
LIGHTWEIGHT_PREVIEW = True

# 视频显示缩放质量
SCALE_QUALITY = "Fast"  # "Fast" or "Smooth"

# 最大轨迹点显示数
MAX_TRAJECTORY_POINTS = 50
```

#### 8.15.3 视频捕获参数

```python
# 默认捕获帧率
DEFAULT_CAPTURE_FPS = 30.0

# 摄像头超时重试次数
CAMERA_RETRY_COUNT = 3

# 视频流重试间隔 (秒)
STREAM_RETRY_INTERVAL = 1.0
```

#### 8.15.4 导出参数

```python
# 默认输出目录
DEFAULT_OUTPUT_DIR = "output"

# CSV 导出文件名前缀
CSV_FILENAME_PREFIX = "tracking_log"

# 视频导出文件名前缀
VIDEO_FILENAME_PREFIX = "tracked"
```

#### 8.15.5 模拟参数

```python
# 模拟帧率 (用于 Mock 演示)
MOCK_FPS = 30.0

# 模拟帧生成间隔 (秒)
MOCK_FRAME_INTERVAL = 1.0 / MOCK_FPS
```

### 8.16 GUI配置热更新机制详解

#### 8.16.1 配置更新流程

```
用户在UI中修改参数
    │
    ▼
MainWindow._on_config_value_changed(key, value)
    │
    ├─> config_changed 信号
    │
    ▼
MainWindow._apply_ui_config_to_pipeline_config()
    │
    ├─> self._pipeline.config.detector.confidence_threshold = ui_value
    ├─> self._pipeline.config.detector.iou_threshold = ui_value
    ├─> self._pipeline.config.detector.device = ui_value
    ├─> self._pipeline.config.visualizer.show_bbox = ui_value
└─> self._pipeline.config.visualizer.show_trajectory = ui_value
```

#### 8.16.2 配置映射表

```python
# UI配置到Pipeline配置的映射
config_map = {
    'confidence_threshold': ('detector', 'confidence_threshold'),
    'iou_threshold': ('detector', 'iou_threshold'),
    'device': ('detector', 'device'),
    'show_bbox': ('visualizer', 'show_bbox'),
    'show_trajectory': ('visualizer', 'show_trajectory'),
    'show_id': ('visualizer', 'show_id'),
    'show_center_point': ('visualizer', 'show_center_point'),
    'show_confidence': ('visualizer', 'show_confidence'),
}
```

#### 8.16.3 热更新时机

```python
# 配置更新在以下时机触发：
# 1. 用户拖动滑块时
# 2. 用户勾选/取消勾选复选框时
# 3. 用户选择下拉框选项时

# 配置更新立即生效，无需重启：
# - 检测置信度：下一帧推理时使用新值
# - IOU阈值：下一帧推理时使用新值
# - 设备选择：需要重新初始化模型（暂不支持热更新）
# - 可视化开关：立即生效
```

#### 8.16.4 限制说明

**不支持热更新的配置**:
- `device`: 需要重新初始化模型
- `model_path`: 需要重新加载模型
- `tracker_type`: 需要重新初始化跟踪器

**支持热更新的配置**:
- `confidence_threshold`: 立即生效
- `iou_threshold`: 立即生效
- `show_bbox`: 立即生效
- `show_trajectory`: 立即生效
- `show_id`: 立即生效
- `show_center_point`: 立即生效
- `show_confidence`: 立即生效

### 8.17 性能指标数据流详解

#### 8.17.1 数据流图

```
Pipeline.process_frame()
    │
    ├─> _metrics_collector.report_frame()
    │   │
    │   ├─> 记录帧ID、推理时间、检测数量
    │   ├─> 更新滑动窗口
    │   └─> 计算FPS
    │
    └─> _metrics_collector.get_snapshot()
        │
        ├─> 返回 PerformanceSnapshot
        │   │
        │   ├─> fps: 实时帧率
        │   ├─> inference_time_ms: 平均推理时间
        │   ├─> detection_count: 检测数量
        │   ├─> frame_count: 总帧数
        │   └─> elapsed_time: 总耗时
```

#### 8.17.2 GUI显示流程

```
Pipeline (Worker线程)
    │
    ├─> _metrics_collector.report_frame()
    │   │
    │   └─> 触发回调
    │       │
    │       └─> listener(snapshot)
    │           │
    │           └─> InferenceWorker.metrics_updated 信号
    │               │
    │               └─> MainWindow._on_metrics_updated()
    │                   │
    │                   └─> 更新状态栏FPS显示
```

#### 8.17.3 FPS显示更新

```python
# MainWindow 中的FPS更新
def _on_metrics_updated(self, metrics: dict):
    """更新性能指标显示"""
    fps = metrics.get('fps', 0.0)
    detection_count = metrics.get('detection_count', 0)
    
    # 更新状态栏
    self._status_bar_fps_label.setText(f"FPS: {fps:.1f}")
    self._status_bar_count_label.setText(f"Objects: {detection_count}")
    
    # 更新性能面板（如果有）
    if self._performance_panel:
        self._performance_panel.update_metrics(metrics)
```

#### 8.17.4 性能指标计算

```python
# FPS计算（基于滑动窗口）
if len(self._frame_history) >= 2:
    time_span = self._frame_history[-1].timestamp - self._frame_history[0].timestamp
    if time_span > 0:
        fps = (len(self._frame_history) - 1) / time_span
    else:
        fps = 0.0

# 平均推理时间
if self._frame_history:
    avg_inference = sum(m.inference_time_ms for m in self._frame_history) / len(self._frame_history)
else:
    avg_inference = 0.0
```

### 8.18 测试架构分析

#### 8.18.1 测试文件结构

```
tests/
├── test_types.py              # 数据类型测试 (20 tests)
├── test_config.py           # 配置测试 (11 tests)
├── test_trajectory.py        # 轨迹管理测试 (9 tests)
├── test_csv_exporter.py      # CSV导出测试 (6 tests)
├── test_parameter_override.py  # 参数边界值测试 (19 tests)
├── test_worker_integration.py  # Worker集成测试 (7 tests)
└── test_export_functionality.py  # 导出功能测试 (12 tests)
```

#### 8.18.2 测试覆盖范围

| 测试文件 | 测试内容 | 测试数量 |
|---------|---------|---------|
| `test_types.py` | BoundingBox, Detection, TrackedObject, Frame, Trajectory | 20 |
| `test_config.py` | 配置加载/保存、参数验证 | 11 |
| `test_trajectory.py` | 轨迹管理、清理失效 | 9 |
| `test_csv_exporter.py` | CSV导出、原子写入 | 6 |
| `test_parameter_override.py` | 参数边界值测试 | 19 |
| `test_worker_integration.py` | Worker线程集成 | 7 |
| `test_export_functionality.py` | 导出功能 | 12 |

**总计**: 91个测试（7个测试文件）

#### 8.18.3 测试运行方式

```bash
# 运行所有测试
cd person_tracking
python -m pytest tests/ -v

# 运行特定测试
python -m pytest tests/test_types.py -v

# 生成覆盖率报告
python -m pytest tests/ --cov=src --cov-report=html
```

#### 8.18.4 测试质量保证

- ✅ 单元测试覆盖核心功能
- ✅ 集成测试覆盖Worker线程
- ✅ 边界值测试覆盖参数范围
- ✅ 导出功能测试覆盖导出流程
- ⚠️ 缺少GUI集成测试
- ⚠️ 缺少端到端测试

---

### 8.19 main.py - CLI入口与Python API

#### 8.19.1 文件概述

`src/main.py` 是整个系统的应用层入口，提供两种使用方式：

1. **命令行接口（CLI）** - 通过 `python -m src.main` 调用
2. **Python API** - 通过 `from src.main import run_tracking` 调用

**文件行数**: 275 行

**核心职责**:
- CLI 参数解析
- 配置加载与参数覆盖
- Pipeline 实例化与执行
- 进度条显示
- 错误处理与统计信息输出

#### 8.19.2 create_parser() - CLI参数解析器

**位置**: 第 26-130 行

**功能**: 创建 argparse.ArgumentParser，定义所有命令行参数

**参数清单**:

| 参数 | 类型 | 必需 | 默认值 | 说明 |
|------|------|------|--------|------|
| `--source` / `-s` | str | ✅ | - | 视频源（文件路径或摄像头索引） |
| `--output` / `-o` | str | ❌ | None | 输出视频路径 |
| `--csv` / `-c` | str | ❌ | None | CSV 日志输出路径 |
| `--config` / `-f` | str | ❌ | None | 配置文件路径（YAML） |
| `--show` | flag | ❌ | False | 实时显示处理结果 |
| `--model` / `-m` | str | ❌ | None | 模型路径（覆盖配置文件） |
| `--device` / `-d` | str | ❌ | None | 推理设备（cuda/cpu/mps） |
| `--confidence` / `-conf` | float | ❌ | None | 检测置信度阈值（覆盖配置文件） |
| `--quiet` / `-q` | flag | ❌ | False | 静默模式，不显示进度 |
| `--version` / `-v` | flag | ❌ | - | 显示版本信息 |

**关键设计**:
- 使用 `RawDescriptionHelpFormatter` 保持示例代码格式
- 提供详细的 epilog 示例代码
- `--device` 使用 `choices` 限制可选值

**代码示例**:
```python
parser = argparse.ArgumentParser(
    description="Person Detection and Tracking System",
    formatter_class=argparse.RawDescriptionHelpFormatter,
    epilog="""
Examples:
    # 处理视频文件
    python -m person_tracking.main --source video.mp4 --output output/tracked.mp4
    
    # 使用摄像头实时处理
    python -m person_tracking.main --source 0 --show
    
    # 指定配置文件
    python -m person_tracking.main --source video.mp4 --config config/custom.yaml
    
    # 导出 CSV 日志
    python -m person_tracking.main --source video.mp4 --csv output/log.csv
        """,
)
```

#### 8.19.3 run_tracking() - Python API函数

**位置**: 第 133-208 行

**功能**: 提供便捷的 Python API，供其他程序调用

**函数签名**:
```python
def run_tracking(
    source: Union[str, int, Path],
    output: Optional[str | Path] = None,
    csv: Optional[str | Path] = None,
    config: Optional[str | Path] = None,
    show: bool = False,
    model: Optional[str] = None,
    device: Optional[str] = None,
    confidence: Optional[float] = None,
    quiet: bool = False,
) -> dict:
```

**返回值**: 处理统计信息字典
```python
{
    'total_frames': int,           # 总帧数
    'elapsed_time': float,         # 处理时间（秒）
    'avg_fps': float,              # 平均帧率
    'trajectories_created': int,   # 创建的轨迹数量
}
```

**执行流程**:

```
1. 加载配置
   └─> cfg = load_config(config)

2. 应用参数覆盖（使用 is not None 判断）
   ├─> if model is not None: cfg.detector.model_path = model
   ├─> if device is not None: cfg.detector.device = device
   └─> if confidence is not None: cfg.detector.confidence_threshold = confidence

3. 创建 Pipeline
   └─> pipeline = TrackingPipeline(cfg)

4. 决定是否显示进度条
   └─> should_show_progress = not quiet and cfg.pipeline.show_progress

5. 定义进度回调
   └─> progress_callback(current, total) -> 显示进度条

6. 运行处理
   └─> stats = pipeline.run(source, output, csv, show, progress_callback)

7. 返回统计信息
   └─> return stats
```

**关键设计决策**:

1. **参数覆盖使用 `is not None` 判断**
   ```python
   # 正确：支持 confidence=0.0 等边缘值
   if confidence is not None:
       cfg.detector.confidence_threshold = confidence

   # 错误：当 confidence=0.0 时会错误跳过
   if confidence:
       cfg.detector.confidence_threshold = confidence
   ```

2. **进度条显示优先级**
   ```python
   # 优先级: quiet 参数 > show_progress 配置 > 默认(True)
   should_show_progress = not quiet and cfg.pipeline.show_progress
   ```

3. **进度回调实现**
   ```python
   def progress_callback(current: int, total: int) -> None:
       if should_show_progress and total > 0:
           percent = current / total * 100
           bar_len = 40
           filled = int(bar_len * current / total)
           bar = "█" * filled + "-" * (bar_len - filled)
           print(f"\r进度: [{bar}] {percent:.1f}% ({current}/{total})", end="")
           if current >= total:
               print()  # 换行
   ```

**使用示例**:
```python
from src.main import run_tracking

# 基本使用
stats = run_tracking(
    source="video.mp4",
    output="output/tracked.mp4",
    csv="output/log.csv",
)

# 覆盖配置参数
stats = run_tracking(
    source="video.mp4",
    model="yolo11s.pt",
    device="cuda",
    confidence=0.6,
)

# 静默模式
stats = run_tracking(
    source="video.mp4",
    quiet=True,
)

# 使用摄像头
stats = run_tracking(
    source=0,  # 摄像头索引
    show=True,
)
```

#### 8.19.4 main() - CLI入口函数

**位置**: 第 211-272 行

**功能**: 命令行入口点，解析参数并调用 `run_tracking()`

**执行流程**:

```
1. 解析参数
   └─> parser = create_parser()
   └─> args = parser.parse_args()

2. 设置日志
   └─> setup_logger(level="WARNING" if args.quiet else "INFO")

3. 处理 source（可能是摄像头索引）
   └─> 尝试解析为整数（摄像头索引）
   └─> 失败则作为文件路径

4. 运行跟踪
   └─> stats = run_tracking(source, output, csv, config, show, model, device, confidence, quiet)

5. 输出统计信息（非静默模式）
   └─> 打印总帧数、处理时间、平均帧率、轨迹数量

6. 返回退出码
   └─> 成功: 0
   └─> 用户中断: 1
   └─> 异常: 1
```

**关键设计**:

1. **source 参数智能解析**
   ```python
   # 尝试解析为整数（摄像头索引）
   try:
       source = int(args.source)
   except ValueError:
       # 文件路径
       source = args.source
   ```

2. **错误处理**
   ```python
   try:
       # ... 处理逻辑 ...
       return 0
   except KeyboardInterrupt:
       print("\n用户中断")
       return 1
   except Exception as e:
       logger.error(f"运行错误: {e}")
       print(f"错误: {e}", file=sys.stderr)
       return 1
   ```

3. **统计信息输出**
   ```python
   if not args.quiet:
       print("\n" + "=" * 50)
       print("处理完成!")
       print(f"  总帧数: {stats['total_frames']}")
       print(f"  处理时间: {stats['elapsed_time']:.2f} 秒")
       print(f"  平均帧率: {stats['avg_fps']:.1f} FPS")
       print(f"  轨迹数量: {stats['trajectories_created']}")
       if args.output:
           print(f"  输出视频: {args.output}")
       if args.csv:
           print(f"  CSV 日志: {args.csv}")
       print("=" * 50)
   ```

#### 8.19.5 关键设计模式

**1. 双接口设计**

main.py 提供两种使用方式，满足不同场景需求：

| 使用方式 | 适用场景 | 优点 |
|---------|---------|------|
| CLI | 脚本调用、批处理 | 无需编写代码，快速测试 |
| Python API | 集成到其他程序 | 灵活控制，可编程 |

**2. 参数覆盖机制**

支持通过命令行参数覆盖配置文件，优先级清晰：

```
命令行参数 > 配置文件 > 默认值
```

**3. 进度回调机制**

通过回调函数实现进度显示，解耦显示逻辑：

```python
# Pipeline 不关心如何显示进度
pipeline.run(progress_callback=callback)

# main.py 决定如何显示进度
def progress_callback(current, total):
    # 显示进度条
```

**4. 错误处理策略**

- **KeyboardInterrupt**: 用户中断，返回退出码 1
- **Exception**: 记录日志并返回退出码 1
- **静默模式**: 不显示进度，但仍然输出错误信息

#### 8.19.6 与其他模块的交互

```
main.py
│
├─> infra.config
│   ├─> load_config() - 加载配置
│   └─> Config - 配置对象
│
├─> infra.logger
│   ├─> setup_logger() - 设置日志
│   └─> get_logger() - 获取日志器
│
└─> core.pipeline
    └─> TrackingPipeline.run() - 运行处理管道
        ├─> VideoLoader - 加载视频
        ├─> PersonDetector - 检测
        ├─> PersonTracker - 跟踪
        ├─> TrajectoryManager - 管理轨迹
        ├─> Visualizer - 可视化
        └─> CSVExporter - 导出CSV
```

#### 8.19.7 潜在问题与改进建议

**问题 1: 缺少参数验证**

**问题描述**: CLI 参数缺少验证，可能导致运行时错误

**示例**:
```python
# 用户可能输入无效的模型路径
python -m src.main --source video.mp4 --model /invalid/path/model.pt

# 或无效的设备
python -m src.main --source video.mp4 --device invalid_device
```

**改进建议**:
```python
def main() -> int:
    parser = create_parser()
    args = parser.parse_args()

    # 添加参数验证
    if args.model and not Path(args.model).exists():
        print(f"错误: 模型文件不存在: {args.model}", file=sys.stderr)
        return 1

    if args.device and args.device not in ["cuda", "cpu", "mps"]:
        print(f"错误: 无效的设备: {args.device}", file=sys.stderr)
        return 1

    # ... 继续处理
```

**问题 2: 缺少详细的帮助信息**

**问题描述**: 某些参数缺少详细说明，用户可能不清楚如何使用

**改进建议**:
```python
parser.add_argument(
    "--confidence",
    "-conf",
    type=float,
    default=None,
    help="检测置信度阈值（0.0-1.0），值越高越严格，默认使用配置文件中的值",
)
```

**问题 3: 缺少批量处理支持**

**问题描述**: 当前只支持单个视频源，无法批量处理多个视频

**改进建议**:
```python
# 添加 --batch 参数
parser.add_argument(
    "--batch",
    "-b",
    type=str,
    help="批量处理模式，指定包含多个视频的目录",
)

# 在 run_tracking 中添加批量处理逻辑
if batch_dir:
    video_files = list(Path(batch_dir).glob("*.mp4"))
    for video_file in video_files:
        stats = run_tracking(source=video_file, ...)
```

**问题 4: 缺少性能分析模式**

**问题描述**: 无法方便地分析性能瓶颈

**改进建议**:
```python
parser.add_argument(
    "--profile",
    action="store_true",
    help="性能分析模式，输出详细的性能统计",
)

# 在 run_tracking 中添加性能分析
if profile:
    import cProfile
    import pstats

    profiler = cProfile.Profile()
    profiler.enable()

    stats = pipeline.run(...)

    profiler.disable()
    stats_obj = pstats.Stats(profiler)
    stats_obj.sort_stats('cumulative')
    stats_obj.print_stats(20)
```

#### 8.19.8 总结

main.py 是整个系统的应用层入口，提供了简洁的 CLI 和 Python API 两种使用方式。其设计遵循了以下原则：

1. **简洁性**: 参数清晰，使用简单
2. **灵活性**: 支持参数覆盖，满足不同场景需求
3. **健壮性**: 完善的错误处理，避免程序崩溃
4. **可扩展性**: 易于添加新参数和功能

**关键亮点**:
- ✅ 双接口设计（CLI + Python API）
- ✅ 参数覆盖机制（命令行 > 配置文件 > 默认值）
- ✅ 进度回调机制（解耦显示逻辑）
- ✅ 智能的 source 参数解析（支持摄像头索引）
- ✅ 完善的错误处理（KeyboardInterrupt + Exception）

**改进空间**:
- ⚠️ 添加参数验证
- ⚠️ 完善帮助信息
- ⚠️ 支持批量处理
- ⚠️ 添加性能分析模式

---

### 8.20 metrics.py - 性能指标收集器

#### 8.20.1 文件概述

`src/infra/metrics.py` 是性能指标收集模块，提供统一的性能指标计算和收集，支持多源数据汇总。用于解决GUI层帧率显示与实际推理结果不匹配的问题。

**文件行数**: 228 行

**核心职责**:
- 统一的性能指标计算中心
- 解决多源FPS计算不一致问题
- 提供滑动窗口平均FPS计算
- 支持回调机制实时通知指标更新

#### 8.20.2 核心数据结构

**FrameMetrics** - 单帧性能指标
```python
@dataclass
class FrameMetrics:
    frame_id: int              # 帧编号
    timestamp: float           # 时间戳
    inference_time_ms: float   # 推理耗时（毫秒）
    detection_count: int       # 检测到的目标数量
```

**PerformanceSnapshot** - 性能指标快照
```python
@dataclass
class PerformanceSnapshot:
    fps: float                 # 帧率
    inference_time_ms: float   # 平均推理时间
    detection_count: int       # 最新检测数
    frame_count: int           # 总帧数
    elapsed_time: float        # 总耗时
```

#### 8.20.3 MetricsCollector 类

**核心功能**:

1. **滑动窗口FPS计算**
   - 使用 `deque` 保存最近N帧的指标
   - 基于时间跨度计算真实FPS
   - 避免瞬时波动影响

2. **线程安全设计**
   - 使用 `threading.Lock` 保护共享数据
   - 回调在锁外触发，避免死锁

3. **回调机制**
   - 支持多个监听器
   - 每次报告帧时自动触发回调

**关键方法**:

| 方法 | 功能 | 线程安全 |
|------|------|---------|
| `start()` | 开始收集 | ✅ |
| `stop()` | 停止收集 | ✅ |
| `report_frame()` | 报告一帧结果 | ✅ |
| `get_snapshot()` | 获取当前快照 | ✅ |
| `add_listener()` | 添加监听器 | ❌ |
| `remove_listener()` | 移除监听器 | ❌ |
| `reset()` | 重置所有指标 | ✅ |

**使用示例**:
```python
# 创建收集器
collector = MetricsCollector(window_size=30)
collector.start()

# Worker中报告
collector.report_frame(
    frame_id=100,
    inference_time_ms=15.5,
    detection_count=3
)

# UI中定期获取
metrics = collector.get_snapshot()
print(f"FPS: {metrics.fps:.1f}")
print(f"推理时间: {metrics.inference_time_ms:.1f}ms")
print(f"检测数: {metrics.detection_count}")
```

#### 8.20.4 FPS计算逻辑

**计算公式**:
```python
# 基于滑动窗口计算FPS
if len(self._frame_history) >= 2:
    time_span = self._frame_history[-1].timestamp - self._frame_history[0].timestamp
    if time_span > 0:
        fps = (len(self._frame_history) - 1) / time_span
    else:
        fps = 0.0
```

**设计亮点**:
- 使用时间跨度而非帧数计算，更准确
- 滑动窗口平滑瞬时波动
- 避免除零错误

#### 8.20.5 全局单例模式

**单例实现**:
```python
_global_collector: Optional[MetricsCollector] = None
_lock = threading.Lock()

def get_metrics_collector() -> MetricsCollector:
    """获取全局MetricsCollector实例"""
    global _global_collector
    if _global_collector is None:
        with _lock:
            if _global_collector is None:
                _global_collector = MetricsCollector()
    return _global_collector
```

**使用场景**:
- 跨组件共享同一个收集器
- 避免重复创建实例
- 统一性能指标来源

#### 8.20.6 与其他模块的交互

```
MetricsCollector
│
├─> VideoCaptureWorker
│   └─> report_frame(frame_id, inference_time, detection_count)
│
├─> InferenceWorker
│   └─> report_frame(frame_id, inference_time, detection_count)
│
└─> MainWindow
    ├─> add_listener(callback)
    └─> get_snapshot() -> 更新UI显示
```

#### 8.20.7 潜在问题与改进建议

**问题 1: 回调异常被静默忽略**

**问题描述**: 回调函数中的异常被捕获并忽略，可能导致问题难以发现

**代码**:
```python
for listener in self._listeners:
    try:
        listener(snapshot)
    except Exception:
        pass  # 静默忽略
```

**改进建议**:
```python
for listener in self._listeners:
    try:
        listener(snapshot)
    except Exception as e:
        logger.error(f"Metrics callback error: {e}")
```

**问题 2: 缺少性能指标历史记录**

**问题描述**: 当前只保留滑动窗口内的数据，无法查看历史趋势

**改进建议**:
```python
# 添加历史记录功能
class MetricsCollector:
    def __init__(self, window_size: int = 30, history_size: int = 1000):
        self._window_size = window_size
        self._history: deque[PerformanceSnapshot] = deque(maxlen=history_size)
    
    def report_frame(self, ...):
        # ... 现有逻辑 ...
        snapshot = self._calculate_snapshot_unlocked()
        self._history.append(snapshot)  # 保存历史
```

#### 8.20.8 总结

metrics.py 提供了统一的性能指标收集机制，解决了多源FPS计算不一致的问题。其设计遵循了以下原则：

1. **统一性**: 单一数据源，避免多源不一致
2. **准确性**: 基于时间跨度计算，更真实
3. **线程安全**: 使用锁保护共享数据
4. **可扩展**: 支持回调机制，易于集成

**关键亮点**:
- ✅ 滑动窗口平滑FPS计算
- ✅ 线程安全设计
- ✅ 回调机制实时通知
- ✅ 全局单例模式

**改进空间**:
- ⚠️ 回调异常应记录日志
- ⚠️ 添加历史记录功能
- ⚠️ 支持性能指标导出

---

### 8.21 constants.py - GUI常量配置

#### 8.21.1 文件概述

`src/gui/constants.py` 集中定义GUI组件的刷新频率和显示参数，避免散落写死。

**文件行数**: 73 行

**核心职责**:
- 集中管理GUI刷新节流参数
- 定义视频显示参数
- 配置视频捕获参数
- 设置导出参数

#### 8.21.2 刷新节流参数

| 参数 | 默认值 | 说明 | 用途 |
|------|--------|------|------|
| `TARGET_TABLE_REFRESH_INTERVAL` | 5 | 目标表格刷新间隔（帧） | 控制目标列表更新频率 |
| `TRAJECTORY_LIST_REFRESH_INTERVAL` | 10 | 轨迹列表刷新间隔（帧） | 控制轨迹列表更新频率 |
| `LOG_PANEL_REFRESH_INTERVAL` | 30 | 日志面板刷新间隔（帧） | 控制日志面板更新频率 |
| `STATUS_BAR_FPS_UPDATE_INTERVAL` | 0.5 | 状态栏FPS更新间隔（秒） | 控制FPS显示更新频率 |

**设计理念**:
- 不同组件使用不同的刷新频率
- 高频组件（目标表格）刷新快
- 低频组件（日志面板）刷新慢
- 避免过度刷新影响性能

#### 8.21.3 视频显示参数

| 参数 | 默认值 | 说明 | 可选值 |
|------|--------|------|--------|
| `LIGHTWEIGHT_PREVIEW` | True | 轻量预览模式 | True/False |
| `SCALE_QUALITY` | "Fast" | 缩放质量 | "Fast"/"Smooth" |
| `MAX_TRAJECTORY_POINTS` | 50 | 最大轨迹点显示数 | 整数 |

**轻量模式 vs 完整模式**:
```python
# 轻量模式（LIGHTWEIGHT_PREVIEW = True）
# 只显示: 边界框 + Track ID

# 完整模式（LIGHTWEIGHT_PREVIEW = False）
# 还显示: 轨迹 + 置信度 + 中心点
```

**缩放质量**:
- **Fast**: 无平滑变换，速度快
- **Smooth**: 平滑变换，质量好但稍慢

#### 8.21.4 视频捕获参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `DEFAULT_CAPTURE_FPS` | 30.0 | 默认捕获帧率 |
| `CAMERA_RETRY_COUNT` | 3 | 摄像头超时重试次数 |
| `STREAM_RETRY_INTERVAL` | 1.0 | 视频流重试间隔（秒） |

#### 8.21.5 导出参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `DEFAULT_OUTPUT_DIR` | "output" | 默认输出目录 |
| `CSV_FILENAME_PREFIX` | "tracking_log" | CSV导出文件名前缀 |
| `VIDEO_FILENAME_PREFIX` | "tracked" | 视频导出文件名前缀 |

#### 8.21.6 其他参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `MOCK_FPS` | 30.0 | 模拟帧率（用于Mock演示） |
| `MOCK_FRAME_INTERVAL` | 1.0/30.0 | 模拟帧生成间隔（秒） |

#### 8.21.7 使用示例

```python
from src.gui.constants import (
    TARGET_TABLE_REFRESH_INTERVAL,
    TRAJECTORY_LIST_REFRESH_INTERVAL,
    LIGHTWEIGHT_PREVIEW,
    SCALE_QUALITY,
)

# 在GUI中使用
class MainWindow(QMainWindow):
    def __init__(self):
        super().__init__()
        
        # 使用常量配置刷新频率
        self._target_table_refresh_counter = 0
        self._trajectory_list_refresh_counter = 0
        
    def update_ui(self, frame_id):
        # 目标表格每5帧刷新一次
        self._target_table_refresh_counter += 1
        if self._target_table_refresh_counter >= TARGET_TABLE_REFRESH_INTERVAL:
            self._update_target_table()
            self._target_table_refresh_counter = 0
        
        # 轨迹列表每10帧刷新一次
        self._trajectory_list_refresh_counter += 1
        if self._trajectory_list_refresh_counter >= TRAJECTORY_LIST_REFRESH_INTERVAL:
            self._update_trajectory_list()
            self._trajectory_list_refresh_counter = 0
```

#### 8.21.8 潜在问题与改进建议

**问题 1: 常量硬编码，无法运行时调整**

**问题描述**: 所有常量都是硬编码的，无法通过配置文件或UI调整

**改进建议**:
```python
# 改为从配置文件读取
from src.infra.config import get_config

config = get_config()
TARGET_TABLE_REFRESH_INTERVAL = config.gui.target_table_refresh_interval
```

**问题 2: 缺少参数验证**

**问题描述**: 没有验证参数的合理性（如负数、过大值）

**改进建议**:
```python
# 添加参数验证
assert TARGET_TABLE_REFRESH_INTERVAL > 0, "刷新间隔必须大于0"
assert MAX_TRAJECTORY_POINTS >= 0, "轨迹点数不能为负"
```

#### 8.21.9 总结

constants.py 集中管理GUI常量配置，避免了散落写死的问题。其设计遵循了以下原则：

1. **集中管理**: 所有常量集中定义
2. **可读性强**: 使用有意义的常量名
3. **易于维护**: 修改时只需改一处
4. **性能优化**: 通过节流避免过度刷新

**关键亮点**:
- ✅ 刷新节流参数集中配置
- ✅ 视频显示参数统一管理
- ✅ 导出参数标准化
- ✅ 代码可读性强

**改进空间**:
- ⚠️ 支持运行时调整
- ⚠️ 添加参数验证
- ⚠️ 支持配置文件覆盖

---

### 8.22 YAML配置文件详解

#### 8.22.1 default.yaml - 默认配置

**文件路径**: `config/default.yaml`

**文件行数**: 127 行

**配置结构**:

```yaml
# 检测器配置
detector:
  model_path: "yolo11n.pt"           # 模型路径
  confidence_threshold: 0.5          # 检测置信度阈值
  iou_threshold: 0.45                 # NMS IOU阈值
  device: "cuda"                      # 推理设备
  classes: [0]                        # 检测类别（COCO: person=0）
  imgsz: 640                          # 推理图像尺寸

# 跟踪器配置
tracker:
  tracker_type: "bytetrack"           # 跟踪器类型
  track_buffer: 30                    # 轨迹缓冲帧数
  match_thresh: 0.8                   # 匹配阈值
  track_thresh: 0.5                   # 跟踪置信度阈值
  new_track_thresh: 0.6               # 新建轨迹阈值

# 可视化配置
visualizer:
  box_color: [0, 255, 0]             # 边界框颜色（BGR）
  text_color: [255, 255, 255]        # 文本颜色（BGR）
  trajectory_color: [255, 0, 0]      # 轨迹颜色（BGR）
  line_thickness: 2                   # 线条宽度
  trajectory_length: 50              # 轨迹显示长度
  show_center_point: true             # 显示中心点
  show_confidence: true              # 显示置信度
  font_scale: 0.6                     # 字体缩放
  show_bbox: true                    # 显示边界框
  show_trajectory: true              # 显示轨迹
  show_id: true                      # 显示ID标签

# 处理管道配置
pipeline:
  show_progress: true                 # 显示进度条
  save_output: true                  # 保存输出视频
  output_fps: null                   # 输出视频帧率
  skip_frames: 0                     # 跳帧处理数量
  warmup: true                       # 模型预热

# 日志配置
logging:
  level: "INFO"                      # 日志级别
  format: "<green>{time:YYYY-MM-DD HH:mm:ss}</green> | ..."  # 日志格式
  file: "logs/tracking.log"          # 日志文件
  rotation: "10 MB"                  # 日志轮转大小
  retention: "7 days"                # 日志保留时间
```

#### 8.22.2 bytetrack.yaml - ByteTrack配置

**文件路径**: `config/bytetrack.yaml`

**配置说明**: ByteTrack跟踪器的专用配置文件，包含跟踪算法的核心参数。

**关键参数**:

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `track_buffer` | 30 | 轨迹缓冲帧数 |
| `match_thresh` | 0.8 | 匹配阈值 |
| `track_thresh` | 0.5 | 跟踪置信度阈值 |
| `new_track_thresh` | 0.6 | 新建轨迹阈值 |

#### 8.22.3 配置项详解

**检测器配置**:

| 配置项 | 类型 | 范围 | 说明 |
|--------|------|------|------|
| `model_path` | str | - | 模型文件路径 |
| `confidence_threshold` | float | [0.0, 1.0] | 检测置信度阈值，值越高越严格 |
| `iou_threshold` | float | [0.0, 1.0] | NMS IOU阈值，用于去除重叠检测框 |
| `device` | str | "cuda"/"cpu"/"mps" | 推理设备 |
| `classes` | list[int] | - | 检测类别列表，COCO中person=0 |
| `imgsz` | int | - | 推理图像尺寸，越大越精确但越慢 |

**跟踪器配置**:

| 配置项 | 类型 | 范围 | 说明 |
|--------|------|------|------|
| `tracker_type` | str | "bytetrack"/"botsort" | 跟踪器类型 |
| `track_buffer` | int | [1, 100] | 轨迹缓冲帧数，值越大跟踪越稳定但延迟越高 |
| `match_thresh` | float | [0.0, 1.0] | 匹配阈值，值越高匹配越严格 |
| `track_thresh` | float | [0.0, 1.0] | 跟踪置信度阈值 |
| `new_track_thresh` | float | [0.0, 1.0] | 新建轨迹阈值 |

**可视化配置**:

| 配置项 | 类型 | 范围 | 说明 |
|--------|------|------|------|
| `box_color` | list[int] | [0, 255] | 边界框颜色（BGR格式） |
| `text_color` | list[int] | [0, 255] | 文本颜色（BGR格式） |
| `trajectory_color` | list[int] | [0, 255] | 轨迹颜色（BGR格式） |
| `line_thickness` | int | [1, 10] | 线条宽度 |
| `trajectory_length` | int | [0, 1000] | 轨迹显示长度，0表示不显示 |
| `show_center_point` | bool | true/false | 是否显示中心点 |
| `show_confidence` | bool | true/false | 是否显示置信度 |
| `font_scale` | float | [0.1, 2.0] | 字体缩放比例 |
| `show_bbox` | bool | true/false | 是否显示边界框 |
| `show_trajectory` | bool | true/false | 是否显示轨迹 |
| `show_id` | bool | true/false | 是否显示ID标签 |

**处理管道配置**:

| 配置项 | 类型 | 范围 | 说明 |
|--------|------|------|------|
| `show_progress` | bool | true/false | 是否显示进度条 |
| `save_output` | bool | true/false | 是否保存输出视频 |
| `output_fps` | float/null | - | 输出视频帧率，null表示使用原视频帧率 |
| `skip_frames` | int | [0, 10] | 跳帧处理数量，0表示不跳帧 |
| `warmup` | bool | true/false | 是否进行模型预热 |

**日志配置**:

| 配置项 | 类型 | 范围 | 说明 |
|--------|------|------|------|
| `level` | str | "DEBUG"/"INFO"/"WARNING"/"ERROR"/"CRITICAL" | 日志级别 |
| `format` | str | - | 日志格式，支持Loguru格式化 |
| `file` | str | - | 日志文件路径 |
| `rotation` | str | - | 日志轮转大小，如"10 MB" |
| `retention` | str | - | 日志保留时间，如"7 days" |

#### 8.22.4 配置使用示例

**CLI使用**:
```bash
# 使用默认配置
python -m src.main --source video.mp4

# 指定配置文件
python -m src.main --source video.mp4 --config config/default.yaml

# 覆盖配置参数
python -m src.main --source video.mp4 --confidence 0.6 --device cuda
```

**Python API使用**:
```python
from src.main import run_tracking

# 使用默认配置
stats = run_tracking(source="video.mp4")

# 指定配置文件
stats = run_tracking(
    source="video.mp4",
    config="config/default.yaml"
)

# 覆盖配置参数
stats = run_tracking(
    source="video.mp4",
    confidence=0.6,
    device="cuda"
)
```

#### 8.22.5 配置优先级

```
命令行参数 > 配置文件 > 默认值
```

**示例**:
```python
# 配置文件中: confidence_threshold = 0.5
# 命令行: --confidence 0.6
# 最终使用: 0.6
```

#### 8.22.6 总结

YAML配置文件提供了灵活的配置管理机制，支持多层级配置和参数覆盖。其设计遵循了以下原则：

1. **灵活性**: 支持配置文件和命令行参数
2. **可读性**: YAML格式易于阅读和修改
3. **可维护性**: 配置集中管理，易于维护
4. **可扩展性**: 易于添加新的配置项

**关键亮点**:
- ✅ 多层级配置支持
- ✅ 参数覆盖机制
- ✅ 详细的配置说明
- ✅ 合理的默认值

**改进空间**:
- ⚠️ 添加配置验证
- ⚠️ 支持配置文件热更新
- ⚠️ 添加配置文件模板

---

### 8.23 docs/目录文档概述

#### 8.23.1 文档清单

`docs/` 目录包含6个Markdown文档，用于项目交接和设计说明：

| 文档 | 用途 | 内容 |
|------|------|------|
| `PROJECT_HANDOFF.md` | 项目交接 | 项目整体说明、架构、使用方式 |
| `GUI_DESIGN_HANDOFF.md` | GUI设计交接 | GUI架构、组件设计、交互流程 |
| `SESSION_HANDOFF.md` | 会话交接 | 会话上下文、待办事项、技术债 |
| `REFACTOR_REPORT.md` | 重构报告 | 重构内容、原因、效果分析 |
| `NEW_SESSION_COMMAND.md` | 新会话命令 | 启动新会话的命令和参数 |
| `系统设计思路文档.md` | 系统设计 | 系统设计思路、技术选型、架构决策 |

#### 8.23.2 文档用途

**PROJECT_HANDOFF.md**:
- 项目整体说明
- 系统架构
- 使用方式
- 技术栈
- 已知问题

**GUI_DESIGN_HANDOFF.md**:
- GUI架构设计
- 组件设计
- 交互流程
- 样式规范
- 已知问题

**SESSION_HANDOFF.md**:
- 会话上下文
- 待办事项
- 技术债
- 下一步计划

**REFACTOR_REPORT.md**:
- 重构内容
- 重构原因
- 重构效果
- 测试结果

**NEW_SESSION_COMMAND.md**:
- 启动新会话的命令
- 参数说明
- 使用示例

**系统设计思路文档.md**:
- 系统设计思路
- 技术选型
- 架构决策
- 设计权衡

#### 8.23.3 文档维护

**更新时机**:
- 重大功能变更后
- 架构调整后
- 技术债修复后
- 会话交接前

**维护原则**:
- 保持文档与代码同步
- 及时更新变更内容
- 记录设计决策
- 说明已知问题

---

### 8.24 修复脚本概述

#### 8.24.1 脚本清单

项目根目录下有多个修复脚本，用于修复代码格式问题：

| 脚本 | 用途 | 说明 |
|------|------|------|
| `fix_indent.py` | 修复缩进 | 修复Python代码缩进问题 |
| `fix_indent_v2.py` | 修复缩进v2 | 修复缩进问题的改进版本 |
| `fix_indent_v3.py` | 修复缩进v3 | 修复缩进问题的进一步改进 |
| `fix_indent_v4.py` | 修复缩进v4 | 修复缩进问题的最终版本 |

#### 8.24.2 脚本用途

这些脚本用于修复代码缩进问题，可能是由于：
- 不同编辑器的缩进设置不一致
- 代码格式化工具的配置问题
- 手动编辑时的缩进错误

#### 8.24.3 使用方式

```bash
# 运行修复脚本
python fix_indent.py

# 指定要修复的文件
python fix_indent.py src/main.py

# 修复整个项目
python fix_indent.py src/
```

#### 8.24.4 注意事项

- 运行前建议备份代码
- 检查修复结果是否符合预期
- 使用版本控制系统跟踪变更
- 确认缩进风格一致性

---

### 8.25 controller.py - GUI控制器

#### 8.25.1 文件概述

`src/gui/controller.py` 是GUI控制器层，负责协调Pipeline、Workers、Config之间的交互，处理业务逻辑（开始/停止/暂停/导出），管理应用状态。

**文件行数**: 209 行

**核心职责**:
- 协调Pipeline、Workers、Config之间的交互
- 处理业务逻辑（开始/停止/暂停/导出）
- 状态管理（运行状态、视频信息）
- 不直接操作UI，通过信号与UI通信

**设计原则**:
- 主线程安全：所有UI更新通过信号发出
- 配置同步：自动将UI配置变更同步到Pipeline
- 状态机：明确的状态转换（就绪→运行→暂停→停止）

#### 8.25.2 核心数据结构

**AppState** - 应用状态枚举
```python
class AppState(Enum):
    IDLE = auto()      # 就绪
    RUNNING = auto()   # 运行中
    PAUSED = auto()    # 已暂停
    ERROR = auto()    # 错误状态
```

**VideoInfo** - 视频信息
```python
@dataclass
class VideoInfo:
    width: int = 0
    height: int = 0
    fps: float = 0.0
    total_frames: int = 0
    current_frame: int = 0
    is_local_video: bool = False
```

#### 8.25.3 TrackingController 类

**信号定义**:
```python
state_changed = Signal(object)      # AppState
frame_ready = Signal(object)       # NDArray[np.uint8]
metrics_updated = Signal(dict)     # 性能指标
error_occurred = Signal(str)       # 错误信息
progress_updated = Signal(int, int) # current, total
```

**核心方法**:

| 方法 | 功能 | 线程安全 |
|------|------|---------|
| `set_source()` | 设置视频源 | ✅ |
| `initialize()` | 初始化控制器 | ✅ |
| `start()` | 开始处理 | ✅ |
| `pause()` | 暂停处理 | ✅ |
| `stop()` | 停止处理 | ✅ |
| `update_config()` | 更新配置（热更新） | ✅ |
| `get_current_source()` | 获取当前视频源 | ✅ |
| `get_config()` | 获取当前配置 | ✅ |

#### 8.25.4 状态机设计

**状态转换图**:
```
    ┌─────────┐
    │  IDLE   │
    └────┬────┘
         │ start()
         ▼
    ┌─────────┐
    │ RUNNING │
    └────┬────┘
         │ pause()
         ▼
    ┌─────────┐
    │  PAUSED │
    └────┬────┘
         │ start()
         ▼
    ┌─────────┐
    │ RUNNING │
    └────┬────┘
         │ stop() / error
         ▼
    ┌─────────┐
    │  IDLE   │
    └─────────┘
```

#### 8.25.5 配置热更新机制

**update_config()方法**:
```python
def update_config(self, key: str, value: Any) -> None:
    # 映射UI配置到Pipeline配置
    config_map = {
        'confidence_threshold': ('detector', 'confidence_threshold'),
        'iou_threshold': ('detector', 'iou_threshold'),
        'device': ('detector', 'device'),
        'show_bbox': ('visualizer', 'show_bbox'),
        'show_trajectory': ('visualizer', 'show_trajectory'),
        'show_id': ('visualizer', 'show_id'),
        'show_center_point': ('visualizer', 'show_center_point'),
        'show_confidence': ('visualizer', 'show_confidence'),
    }
    
    if key in config_map:
        section, attr = config_map[key]
        setattr(getattr(self._config, section), attr, value)
        
        # 同步更新Pipeline
        if self._pipeline and hasattr(self._pipeline, 'visualizer'):
            viz_config = self._pipeline.visualizer.config
            if hasattr(viz_config, attr):
                setattr(viz_config, attr, value)
```

#### 8.25.6 与其他模块的交互

```
TrackingController
│
├─> MainWindow (UI层)
│   ├─> state_changed 信号
│   ├─> frame_ready 信号
│   ├─> metrics_updated 信号
│   ├─> error_occurred 信号
│   └─> progress_updated 信号
│
├─> Config (配置层)
│   └─> load_config() / update_config()
│
└─> TrackingPipeline (核心层)
    └─> start() / stop() / pause()
```

#### 8.25.7 潜在问题与改进建议

**问题 1: Workers未实际初始化**

**问题描述**: `_capture_worker`和`_inference_worker`在`__init__`中声明为None，但在`start()`中未实际初始化

**改进建议**:
```python
def start(self) -> bool:
    if self._state == AppState.RUNNING:
        return True
    
    if not self._current_source:
        self.error_occurred.emit("未选择视频源")
        return False
    
    try:
        # 初始化Workers
        self._capture_worker = VideoCaptureWorker(self._current_source)
        self._inference_worker = InferenceWorker(self._pipeline)
        
        # 连接信号
        self._capture_worker.frame_ready.connect(self._on_frame_ready)
        self._inference_worker.result_ready.connect(self._on_result_ready)
        
        self._set_state(AppState.RUNNING)
        return True
    except Exception as e:
        self.error_occurred.emit(f"启动失败: {str(e)}")
        self._set_state(AppState.ERROR)
        return False
```

**问题 2: 缺少错误恢复机制**

**问题描述**: 进入ERROR状态后无法恢复

**改进建议**:
```python
def recover(self) -> bool:
    """从错误状态恢复"""
    if self._state != AppState.ERROR:
        return False
    
    try:
        self._cleanup()
        self._set_state(AppState.IDLE)
        return True
    except Exception as e:
        self.error_occurred.emit(f"恢复失败: {str(e)}")
        return False
```

#### 8.25.8 总结

controller.py是GUI控制层的核心，负责协调各模块交互和管理应用状态。其设计遵循了以下原则：

1. **主线程安全**: 所有UI更新通过信号发出
2. **状态管理**: 明确的状态转换逻辑
3. **配置同步**: 自动将UI配置变更同步到Pipeline
4. **职责分离**: 不直接操作UI，通过信号通信

**关键亮点**:
- ✅ 清晰的状态机设计
- ✅ 信号-槽机制实现线程安全
- ✅ 配置热更新支持
- ✅ 错误处理机制

**改进空间**:
- ⚠️ Workers实际初始化逻辑缺失
- ⚠️ 缺少错误恢复机制
- ⚠️ 缺少状态转换日志

---

### 8.26 app.py - GUI应用入口

#### 8.26.1 文件概述

`src/gui/app.py` 是GUI应用程序入口点，负责QApplication初始化和配置、主题和样式加载、窗口创建和显示、应用生命周期管理。

**文件行数**: 120 行

**核心职责**:
- QApplication初始化和配置
- 主题和样式加载
- 窗口创建和显示
- 应用生命周期管理

#### 8.26.2 核心函数

**get_stylesheet()** - 获取样式表
```python
def get_stylesheet() -> str:
    """获取样式表内容"""
    style_path = Path(__file__).parent / "resources" / "styles" / "dark.qss"
    
    if style_path.exists():
        with open(style_path, "r", encoding="utf-8") as f:
            return f.read()
    
    return ""
```

**create_application()** - 创建QApplication实例
```python
def create_application(argv: Optional[list] = None) -> QApplication:
    """创建QApplication实例"""
    if argv is None:
        argv = sys.argv
    
    app = QApplication(argv)
    
    # 设置应用属性
    app.setApplicationName("Person Tracking System")
    app.setApplicationVersion("0.1.0")
    app.setOrganizationName("PersonTracking")
    app.setOrganizationDomain("persontracking.local")
    
    # 设置全局字体
    font = QFont("Microsoft YaHei", 10)
    app.setFont(font)
    
    # 加载样式表
    style = get_stylesheet()
    if style:
        app.setStyleSheet(style)
    
    return app
```

**create_main_window()** - 创建主窗口
```python
def create_main_window() -> "MainWindow":
    """创建主窗口"""
    from .main_window import MainWindow
    
    window = MainWindow()
    return window
```

**run_gui()** - 运行GUI应用
```python
def run_gui(argv: Optional[list] = None) -> int:
    """运行GUI应用"""
    app = create_application(argv)
    window = create_main_window()
    window.show()
    return app.exec()
```

#### 8.26.3 使用方式

**命令行启动**:
```bash
python -m src.gui.app
```

**代码调用**:
```python
from src.gui import run_gui
run_gui()
```

#### 8.26.4 潜在问题与改进建议

**问题 1: 缺少启动画面**

**问题描述**: 应用启动时没有启动画面，用户体验不佳

**改进建议**:
```python
def run_gui(argv: Optional[list] = None) -> int:
    """运行GUI应用"""
    app = create_application(argv)
    
    # 添加启动画面
    splash = QSplashScreen(QPixmap(":/images/splash.png"))
    splash.show()
    
    # 模拟加载
    QTimer.singleShot(2000, lambda: splash.close())
    
    window = create_main_window()
    window.show()
    
    return app.exec()
```

**问题 2: 缺少异常处理**

**问题描述**: 窗口创建失败时没有异常处理

**改进建议**:
```python
def run_gui(argv: Optional[list] = None) -> int:
    """运行GUI应用"""
    try:
        app = create_application(argv)
        window = create_main_window()
        window.show()
        return app.exec()
    except Exception as e:
        print(f"GUI启动失败: {e}")
        return 1
```

#### 8.26.5 总结

app.py是GUI应用的入口点，负责应用的初始化和生命周期管理。其设计遵循了以下原则：

1. **简洁性**: 提供简单的API启动GUI
2. **可配置性**: 支持样式表和字体配置
3. **可扩展性**: 易于添加启动画面等功能

**关键亮点**:
- ✅ 清晰的函数职责划分
- ✅ 样式表支持
- ✅ 延迟导入避免循环依赖

**改进空间**:
- ⚠️ 添加启动画面
- ⚠️ 添加异常处理
- ⚠️ 添加命令行参数支持

---

### 8.27 tracker.py - 跟踪器

#### 8.27.1 文件概述

`src/core/tracker.py` 封装ByteTrack跟踪算法，提供跨帧目标关联、track_id维护、结果转换为标准TrackedObject格式。

**文件行数**: 308 行

**核心职责**:
- 跨帧目标关联
- track_id维护
- 结果转换为标准TrackedObject格式

#### 8.27.2 PersonTracker 类

**核心功能**:
- 封装ByteTrack跟踪算法
- 基于Ultralytics内置跟踪器实现
- 将检测结果转换为TrackedObject

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `update()` | 更新跟踪状态 | 根据检测结果更新跟踪状态 |
| `reset()` | 重置跟踪器状态 | 清空所有跟踪状态 |
| `get_tracker_config()` | 获取跟踪器配置 | 返回配置字典 |

**update()方法**:
```python
def update(
    self,
    detections: list[Detection],
    frame: NDArray[np.uint8],
    frame_id: int = 0,
    timestamp: float = 0.0,
) -> list[TrackedObject]:
    """更新跟踪状态"""
    tracked_objects = []
    
    for detection in detections:
        if detection.track_id is not None:
            tracked_obj = TrackedObject(
                track_id=detection.track_id,
                detection=detection,
                frame_id=frame_id,
                timestamp=timestamp,
            )
            tracked_objects.append(tracked_obj)
    
    self._frame_count += 1
    return tracked_objects
```

#### 8.27.3 YOLOTrackerWrapper 类

**核心功能**:
- 直接使用Ultralytics YOLO的track()方法
- 一体化检测+跟踪
- 更简单的使用方式

**track()方法**:
```python
def track(
    self,
    frame: NDArray[np.uint8],
    frame_id: int = 0,
    timestamp: float = 0.0,
    confidence: float = 0.5,
    classes: list[int] = None,
) -> list[TrackedObject]:
    """检测并跟踪"""
    if classes is None:
        classes = [0]  # 默认只检测person
    
    try:
        # 使用YOLO的track方法
        results = self.model.track(
            frame,
            conf=confidence,
            classes=classes,
            tracker=f"{self.tracker_config.tracker_type}.yaml",
            persist=True,
            verbose=False,
        )
        
        # 转换结果
        tracked_objects = self._convert_results(results, frame_id, timestamp)
        
        self._frame_count += 1
        return tracked_objects
    except Exception as e:
        raise InferenceError(
            f"Tracking inference failed",
            frame_id=frame_id,
            details=e,
        )
```

#### 8.27.4 跟踪器配置

**ByteTrack配置参数**:
```python
self._tracker_config = {
    "tracker_type": self.config.tracker_type,      # "bytetrack"
    "track_buffer": self.config.track_buffer,      # 30
    "match_thresh": self.config.match_thresh,      # 0.8
    "track_thresh": self.config.track_thresh,      # 0.5
    "new_track_thresh": self.config.new_track_thresh,  # 0.6
}
```

#### 8.27.5 与其他模块的交互

```
tracker.py
│
├─> detector.py
│   └─> 接收检测结果
│
├─> pipeline.py
│   └─> 被Pipeline调用
│
└─> types.py
    └─> 返回TrackedObject
```

#### 8.27.6 潜在问题与改进建议

**问题 1: PersonTracker.update()逻辑简化**

**问题描述**: PersonTracker.update()只是简单转换已有track_id，未实际进行跟踪

**改进建议**:
```python
def update(
    self,
    detections: list[Detection],
    frame: NDArray[np.uint8],
    frame_id: int = 0,
    timestamp: float = 0.0,
) -> list[TrackedObject]:
    """更新跟踪状态"""
    # 实际跟踪逻辑应由YOLOTrackerWrapper处理
    # PersonTracker主要用于兼容性
    tracked_objects = []
    
    for detection in detections:
        if detection.track_id is not None:
            tracked_obj = TrackedObject(
                track_id=detection.track_id,
                detection=detection,
                frame_id=frame_id,
                timestamp=timestamp,
            )
            tracked_objects.append(tracked_obj)
    
    self._frame_count += 1
    return tracked_objects
```

#### 8.27.7 总结

tracker.py封装了ByteTrack跟踪算法，提供了两种使用方式：PersonTracker（兼容性）和YOLOTrackerWrapper（一体化）。其设计遵循了以下原则：

1. **封装性**: 封装ByteTrack算法细节
2. **兼容性**: 提供多种使用方式
3. **标准化**: 统一输出TrackedObject格式

**关键亮点**:
- ✅ 封装ByteTrack算法
- ✅ 支持一体化检测+跟踪
- ✅ 标准化输出格式

**改进空间**:
- ⚠️ PersonTracker逻辑简化
- ⚠️ 缺少跟踪质量评估

---

### 8.28 loader.py - 视频加载器

#### 8.28.1 文件概述

`src/data/loader.py` 提供视频文件和摄像头流的统一加载接口，支持视频文件读取（MP4, AVI, MOV等）、摄像头实时流读取、帧迭代器接口、自动资源管理。

**文件行数**: 408 行

**核心职责**:
- 视频文件读取
- 摄像头实时流读取
- 帧迭代器接口
- 自动资源管理

#### 8.28.2 VideoLoader 类

**核心功能**:
- 统一的视频文件和摄像头加载接口
- 支持帧迭代器
- 自动资源管理（上下文管理器）
- 视频信息获取

**关键属性**:

| 属性 | 类型 | 说明 |
|------|------|------|
| `source` | Union[str, int, Path] | 视频源 |
| `fps` | float | 视频帧率 |
| `frame_count` | int | 总帧数（摄像头返回-1） |
| `width` | int | 视频宽度 |
| `height` | int | 视频高度 |
| `is_camera` | bool | 是否为摄像头源 |

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `read_frame()` | 读取单帧 | 返回Frame对象 |
| `seek()` | 跳转到指定帧 | 仅视频文件有效 |
| `release()` | 释放资源 | 关闭视频捕获 |
| `get_info()` | 获取视频信息 | 返回信息字典 |

**read_frame()方法**:
```python
def read_frame(self) -> Optional[Frame]:
    """读取单帧"""
    if self._cap is None or not self._cap.isOpened():
        return None
    
    # 读取帧
    ret, image = self._cap.read()
    
    if not ret:
        return None
    
    # 计算时间戳
    timestamp = self._frame_id / self.fps if self.fps > 0 else 0.0
    
    # 创建Frame对象
    frame = Frame(
        frame_id=self._frame_id,
        timestamp=timestamp,
        image=image,
    )
    
    self._frame_id += 1
    return frame
```

#### 8.28.3 VideoWriter 类

**核心功能**:
- 将处理后的帧写入视频文件
- 支持多种编码器
- 自动调整尺寸

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `write()` | 写入单帧 | 自动调整尺寸 |
| `release()` | 释放资源 | 关闭视频写入器 |

**write()方法**:
```python
def write(self, frame: NDArray[np.uint8]) -> bool:
    """写入单帧"""
    if self._writer is None:
        return False
    
    # 检查尺寸是否匹配
    h, w = frame.shape[:2]
    if (w, h) != self.size:
        # 调整尺寸
        frame = cv2.resize(frame, self.size)
    
    self._writer.write(frame)
    self._frame_count += 1
    return True
```

#### 8.28.4 使用示例

**从视频文件加载**:
```python
loader = VideoLoader("video.mp4")
for frame in loader:
    print(f"Frame {frame.frame_id}: shape={frame.image.shape}")
loader.release()
```

**从摄像头加载**:
```python
loader = VideoLoader(0)  # 默认摄像头
for frame in loader:
    cv2.imshow("Camera", frame.image)
    if cv2.waitKey(1) == ord('q'):
        break
loader.release()
```

**使用上下文管理器**:
```python
with VideoLoader("video.mp4") as loader:
    for frame in loader:
        process(frame)
```

#### 8.28.5 与其他模块的交互

```
loader.py
│
├─> pipeline.py
│   └─> 提供视频帧
│
├─> types.py
│   └─> 返回Frame对象
│
└─> exceptions.py
    └─> 抛出VideoLoadError
```

#### 8.28.6 潜在问题与改进建议

**问题 1: 缺少帧缓存机制**

**问题描述**: 没有帧缓存，无法回放或随机访问

**改进建议**:
```python
class VideoLoader:
    def __init__(self, source, cache_size=100):
        self._cache = deque(maxlen=cache_size)
    
    def read_frame(self) -> Optional[Frame]:
        frame = self._read_frame_impl()
        if frame:
            self._cache.append(frame)
        return frame
    
    def get_cached_frame(self, frame_id: int) -> Optional[Frame]:
        """从缓存获取帧"""
        for frame in reversed(self._cache):
            if frame.frame_id == frame_id:
                return frame
        return None
```

**问题 2: 缺少错误恢复**

**问题描述**: 读取失败后无法恢复

**改进建议**:
```python
def read_frame(self) -> Optional[Frame]:
    """读取单帧"""
    if self._cap is None or not self._cap.isOpened():
        # 尝试重新初始化
        try:
            self._init_capture()
        except VideoLoadError:
            return None
    
    ret, image = self._cap.read()
    if not ret:
        return None
    
    # ... 其余逻辑 ...
```

#### 8.28.7 总结

loader.py提供了统一的视频加载接口，支持视频文件和摄像头。其设计遵循了以下原则：

1. **统一性**: 统一的接口支持多种视频源
2. **易用性**: 提供迭代器和上下文管理器
3. **健壮性**: 自动资源管理

**关键亮点**:
- ✅ 统一的视频加载接口
- ✅ 支持迭代器和上下文管理器
- ✅ 自动资源管理
- ✅ 视频信息获取

**改进空间**:
- ⚠️ 添加帧缓存机制
- ⚠️ 添加错误恢复
- ⚠️ 支持更多视频格式

---

### 8.29 logger.py - 日志系统

#### 8.29.1 文件概述

`src/infra/logger.py` 提供统一的日志配置和管理，支持多级别日志、文件轮转、彩色输出。

**文件行数**: 120 行

**核心职责**:
- 日志配置
- 日志器获取
- 日志级别管理

#### 8.29.2 核心函数

**setup_logger()** - 设置日志
```python
def setup_logger(
    level: str = "INFO",
    log_file: Optional[str] = None,
    rotation: str = "10 MB",
    retention: str = "7 days",
) -> None:
    """设置日志系统"""
    from loguru import logger
    
    # 移除默认处理器
    logger.remove()
    
    # 添加控制台处理器（彩色输出）
    logger.add(
        sys.stderr,
        format="<green>{time:YYYY-MM-DD HH:mm:ss}</green> | <level>{level: <8}</level> | <cyan>{name}</cyan>:<cyan>{function}</cyan>:<cyan>{line}</cyan> - <level>{message}</level>",
        level=level,
        colorize=True,
    )
    
    # 添加文件处理器
    if log_file:
        logger.add(
            log_file,
            format="{time:YYYY-MM-DD HH:mm:ss} | {level: <8} | {name}:{function}:{line} - {message}",
            level=level,
            rotation=rotation,
            retention=retention,
        )
```

**get_logger()** - 获取日志器
```python
def get_logger(name: str) -> "Logger":
    """获取日志器"""
    from loguru import logger
    return logger.bind(name=name)
```

#### 8.29.3 日志级别

| 级别 | 用途 | 示例 |
|------|------|------|
| DEBUG | 调试信息 | 函数参数、中间结果 |
| INFO | 一般信息 | 系统启动、配置加载 |
| WARNING | 警告信息 | 配置使用默认值 |
| ERROR | 错误信息 | 推理失败、文件读取失败 |
| CRITICAL | 严重错误 | 模型加载失败 |

#### 8.29.4 使用示例

**基本使用**:
```python
from src.infra.logger import get_logger

logger = get_logger("my_module")
logger.info("System started")
logger.warning("Using default config")
logger.error("Failed to load model")
```

**结构化日志**:
```python
logger.info(
    "Processing frame",
    frame_id=100,
    detection_count=5,
    inference_time=15.5
)
```

#### 8.29.5 潜在问题与改进建议

**问题 1: 缺少日志过滤**

**问题描述**: 无法按模块过滤日志

**改进建议**:
```python
def setup_logger(
    level: str = "INFO",
    log_file: Optional[str] = None,
    rotation: str = "10 MB",
    retention: str = "7 days",
    filter_modules: Optional[list[str]] = None,
) -> None:
    """设置日志系统"""
    from loguru import logger
    
    logger.remove()
    
    # 添加过滤器
    def filter_func(record):
        if filter_modules:
            return any(record["name"].startswith(m) for m in filter_modules)
        return True
    
    logger.add(
        sys.stderr,
        filter=filter_func,
        # ... 其他配置 ...
    )
```

#### 8.29.6 总结

logger.py提供了统一的日志配置和管理。其设计遵循了以下原则：

1. **统一性**: 统一的日志配置
2. **灵活性**: 支持多级别日志
3. **易用性**: 简单的API

**关键亮点**:
- ✅ 统一的日志配置
- ✅ 支持文件轮转
- ✅ 彩色控制台输出

**改进空间**:
- ⚠️ 添加日志过滤
- ⚠️ 添加日志统计
- ⚠️ 支持远程日志

---

### 8.30 GUI组件详细分析

#### 8.30.1 trajectory_list.py - 轨迹列表组件

**文件行数**: 174 行

**核心功能**:
- 显示选中目标的轨迹历史记录
- 支持ID选择和数据更新
- 显示轨迹点数和持续时间

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `update_trajectories()` | 更新轨迹数据 | 更新ID下拉框和显示 |
| `set_current_track()` | 设置当前轨迹ID | 在下拉框中选择 |
| `clear()` | 清空数据 | 清空所有数据 |

**update_trajectories()方法**:
```python
def update_trajectories(self, trajectories: dict[int, list]) -> None:
    """更新轨迹数据"""
    self._trajectories = trajectories
    
    # 更新ID下拉框
    current_ids = set(self._trajectories.keys())
    old_ids = {self._id_combo.itemData(i) for i in range(self._id_combo.count())}
    
    # 添加新ID
    for tid in current_ids - old_ids:
        self._id_combo.addItem(f"ID: {tid}", tid)
    
    # 移除不存在的ID
    for i in range(self._id_combo.count() - 1, -1, -1):
        if self._id_combo.itemData(i) not in current_ids:
            self._id_combo.removeItem(i)
    
    # 更新当前显示
    self._update_display()
```

#### 8.30.2 log_panel.py - 日志面板组件

**文件行数**: 189 行

**核心功能**:
- 显示系统运行日志
- 支持日志级别过滤
- 自动滚动到最新日志

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `append_log()` | 添加日志 | 添加日志到面板 |
| `clear()` | 清空日志 | 清空所有日志 |
| `set_level_filter()` | 设置级别过滤 | 过滤日志级别 |

#### 8.30.3 GUI组件交互

```
GUI组件
│
├─> MainWindow
│   ├─> TrajectoryList
│   │   └─> 显示轨迹历史
│   └─> LogPanel
│       └─> 显示系统日志
│
└─> TrackingController
    └─> 通过信号更新UI
```

#### 8.30.4 潜在问题与改进建议

**问题 1: 轨迹列表性能问题**

**问题描述**: 轨迹点过多时列表更新缓慢

**改进建议**:
```python
def _update_display(self) -> None:
    """更新显示内容"""
    self._point_list.clear()
    
    if self._current_track_id is None or self._current_track_id not in self._trajectories:
        self._info_label.setText("点数: 0 | 持续: 0.0s")
        return
    
    points = self._trajectories[self._current_track_id]
    
    # 限制显示数量
    max_display = 100
    recent_points = points[-max_display:] if len(points) > max_display else points
    
    # 批量添加
    self._point_list.setUpdatesEnabled(False)
    for i, (x, y, t) in enumerate(reversed(recent_points)):
        item = QListWidgetItem(f"({int(x)}, {int(y)}) @ {t:.3f}s")
        self._point_list.addItem(item)
    self._point_list.setUpdatesEnabled(True)
```

#### 8.30.5 总结

GUI组件提供了用户友好的界面，支持轨迹查看和日志显示。其设计遵循了以下原则：

1. **易用性**: 简单直观的界面
2. **实时性**: 实时更新数据
3. **可配置性**: 支持级别过滤

**关键亮点**:
- ✅ 轨迹历史显示
- ✅ 日志实时显示
- ✅ ID选择功能

**改进空间**:
- ⚠️ 优化性能
- ⚠️ 添加搜索功能
- ⚠️ 支持导出轨迹

---

### 8.31 video_canvas.py - 视频画布组件

#### 8.31.1 文件概述

`src/gui/widgets/video_canvas.py` 是视频画布组件，用于显示已由Visualizer渲染好的帧，不再负责任何检测框、轨迹、ID的绘制。

**文件行数**: 339 行

**核心职责**:
- 显示已由Pipeline/Visualizer渲染好的帧
- 高质量缩放显示（SmoothTransformation）
- 鼠标交互（缩放、平移）
- 信息文本显示（帧号、FPS等）

**重构说明**:
- 移除了`_draw_overlay`方法（不再二次绘制）
- 移除了所有OpenCV绘制代码
- 移除了硬编码颜色常量（颜色由VisualizerConfig控制）
- `set_frame`现在只接收已渲染的帧
- 缩放改为高质量模式（SmoothTransformation）

#### 8.31.2 核心功能

**信号定义**:
```python
frame_clicked = Signal(int, int)  # 点击坐标
zoom_changed = Signal(float)  # 缩放因子变化
```

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `set_frame()` | 设置当前显示的帧 | 必须是已渲染的帧 |
| `set_info_text()` | 设置信息文本 | 显示在左上角 |
| `clear()` | 清除画布 | 清空所有内容 |
| `set_zoom()` | 设置缩放因子 | 缩放显示 |
| `reset_zoom()` | 重置缩放 | 适应窗口大小 |
| `fit_to_window()` | 适应窗口 | 自动缩放 |

**set_frame()方法**:
```python
def set_frame(self, frame: NDArray[np.uint8]) -> None:
    """设置当前显示的帧（必须是已渲染的帧）
    
    重要：传入的 frame 必须已由 Visualizer.render() 渲染完成，
    包含检测框、轨迹、ID、文字等所有可视化元素。
    本方法不负责任何绘制，只负责显示。
    """
    if frame is None or frame.size == 0:
        return
    
    self._current_frame = frame
    self._update_display()
```

#### 8.31.3 高质量渲染

**缩放模式**:
```python
# 使用 SmoothTransformation 代替 FastTransformation
transform_mode = (
    Qt.TransformationMode.SmoothTransformation
    if self._high_quality_render
    else Qt.TransformationMode.FastTransformation
)

scaled_pixmap = QPixmap.fromImage(q_image).scaled(
    scaled_w, scaled_h,
    Qt.AspectRatioMode.KeepAspectRatio,
    transform_mode
)
```

**显式居中显示**:
```python
# 修复：显式居中显示，防止位置跳动
# AlignCenter 只对 pixmap 内容有效，对 label 自身位置无效
center_x = (self.width() - scaled_pixmap.width()) // 2
center_y = (self.height() - scaled_pixmap.height()) // 2
self._image_label.move(center_x, center_y)
```

#### 8.31.4 鼠标交互

**缩放**:
```python
def wheelEvent(self, event: QWheelEvent) -> None:
    """滚轮事件 - 缩放"""
    delta = event.angleDelta().y()
    zoom_delta = 0.1 if delta > 0 else -0.1
    new_zoom = self._zoom_factor + zoom_delta
    self.set_zoom(new_zoom)
```

**平移**:
```python
def mouseMoveEvent(self, event: QMouseEvent) -> None:
    """鼠标移动事件"""
    if self._is_panning:
        delta = event.pos() - self._last_mouse_pos
        self._pan_offset += delta
        self._last_mouse_pos = event.pos()
        self._image_label.move(
            self._pan_offset.x() + (self.width() - self._image_label.width()) // 2,
            self._pan_offset.y() + (self.height() - self._image_label.height()) // 2
        )
```

#### 8.31.5 与其他模块的交互

```
video_canvas.py
│
├─> MainWindow
│   └─> 显示渲染后的帧
│
└─> Visualizer
    └─> 接收已渲染的帧
```

#### 8.31.6 潜在问题与改进建议

**问题 1: 缺少帧缓存**

**问题描述**: 没有帧缓存，无法回放

**改进建议**:
```python
def __init__(self, parent: Optional[QWidget] = None) -> None:
    super().__init__(parent)
    self._frame_cache = deque(maxlen=100)  # 缓存最近100帧

def set_frame(self, frame: NDArray[np.uint8]) -> None:
    self._frame_cache.append(frame)
    self._current_frame = frame
    self._update_display()

def get_cached_frame(self, frame_id: int) -> Optional[NDArray[np.uint8]]:
    """从缓存获取帧"""
    for frame in reversed(self._frame_cache):
        if frame.frame_id == frame_id:
            return frame
    return None
```

#### 8.31.7 总结

video_canvas.py是纯显示层组件，负责高质量显示已渲染的帧。其设计遵循了以下原则：

1. **单一职责**: 只负责显示，不负责绘制
2. **高质量**: 使用SmoothTransformation保证视觉质量
3. **交互性**: 支持缩放和平移
4. **稳定性**: 显式居中防止位置跳动

**关键亮点**:
- ✅ 纯显示层设计
- ✅ 高质量缩放
- ✅ 鼠标交互支持
- ✅ 显式居中显示

**改进空间**:
- ⚠️ 添加帧缓存
- ⚠️ 支持截图功能
- ⚠️ 支持全屏显示

---

### 8.32 visualizer.py - 可视化渲染器

#### 8.32.1 文件概述

`src/viz/visualizer.py` 提供检测结果的渲染和可视化，支持边界框绘制、track_id标签显示、轨迹线绘制、中心点标记、置信度显示。

**文件行数**: 301 行

**核心职责**:
- 边界框绘制
- track_id标签显示
- 轨迹线绘制
- 中心点标记
- 置信度显示

#### 8.32.2 Visualizer 类

**核心功能**:
- 渲染检测结果
- 绘制轨迹
- 显示ID标签
- 显示置信度

**关键方法**:

| 方法 | 功能 | 说明 |
|------|------|------|
| `render()` | 渲染帧 | 绘制所有可视化元素 |
| `draw_bbox()` | 绘制边界框 | 绘制检测框 |
| `draw_trajectory()` | 绘制轨迹 | 绘制轨迹线 |
| `draw_id()` | 绘制ID标签 | 显示track_id |
| `draw_center_point()` | 绘制中心点 | 显示中心点 |

**render()方法**:
```python
def render(
    self,
    frame: NDArray[np.uint8],
    tracked_objects: list[TrackedObject],
    trajectories: dict[int, Trajectory],
) -> NDArray[np.uint8]:
    """渲染帧
    
    Args:
        frame: 原始帧
        tracked_objects: 跟踪对象列表
        trajectories: 轨迹字典
    
    Returns:
        渲染后的帧
    """
    # 复制帧
    rendered = frame.copy()
    
    # 绘制轨迹
    if self.config.show_trajectory:
        for track_id, traj in trajectories.items():
            self._draw_trajectory(rendered, traj)
    
    # 绘制检测框和ID
    for obj in tracked_objects:
        if self.config.show_bbox:
            self._draw_bbox(rendered, obj.detection.bbox)
        
        if self.config.show_id:
            self._draw_id(rendered, obj)
        
        if self.config.show_center_point:
            self._draw_center_point(rendered, obj.detection.bbox)
        
        if self.config.show_confidence:
            self._draw_confidence(rendered, obj.detection)
    
    return rendered
```

#### 8.32.3 配置控制

**VisualizerConfig**:
```python
@dataclass
class VisualizerConfig:
    box_color: tuple = (0, 255, 0)
    text_color: tuple = (255, 255, 255)
    trajectory_color: tuple = (255, 0, 0)
    line_thickness: int = 2
    trajectory_length: int = 50
    show_center_point: bool = True
    show_confidence: bool = True
    font_scale: float = 0.6
    show_bbox: bool = True
    show_trajectory: bool = True
    show_id: bool = True
```

#### 8.32.4 颜色分配

**颜色映射**:
```python
def _get_color(self, track_id: int) -> tuple:
    """根据track_id获取颜色"""
    if track_id not in self._color_map:
        # 生成随机颜色
        color = (
            random.randint(0, 255),
            random.randint(0, 255),
            random.randint(0, 255),
        )
        self._color_map[track_id] = color
    
    return self._color_map[track_id]
```

#### 8.32.5 与其他模块的交互

```
visualizer.py
│
├─> pipeline.py
│   └─> 被Pipeline调用渲染
│
├─> video_canvas.py
│   └─> 渲染后的帧传给画布显示
│
└─> types.py
    └─> 接收TrackedObject和Trajectory
```

#### 8.32.6 潜在问题与改进建议

**问题 1: 颜色映射无上限**

**问题描述**: `_color_map`字典无上限，长时间运行会占用大量内存

**改进建议**:
```python
def __init__(self, config: VisualizerConfig):
    self.config = config
    self._color_map: dict[int, tuple] = {}
    self._max_color_map_size = 1000  # 限制颜色映射大小

def _get_color(self, track_id: int) -> tuple:
    if track_id not in self._color_map:
        if len(self._color_map) >= self._max_color_map_size:
            # 移除最旧的条目
            oldest_id = next(iter(self._color_map))
            del self._color_map[oldest_id]
        
        color = self._generate_color(track_id)
        self._color_map[track_id] = color
    
    return self._color_map[track_id]
```

#### 8.32.7 总结

visualizer.py是可视化渲染的核心组件，负责绘制所有可视化元素。其设计遵循了以下原则：

1. **配置驱动**: 所有显示元素都可通过配置控制
2. **性能优化**: 使用OpenCV绘制，性能高效
3. **可扩展**: 易于添加新的可视化元素

**关键亮点**:
- ✅ 配置驱动设计
- ✅ 支持多种可视化元素
- ✅ 颜色自动分配
- ✅ 高性能渲染

**改进空间**:
- ⚠️ 限制颜色映射大小
- ⚠️ 添加自定义颜色支持
- ⚠️ 优化绘制性能

---

## 附录

### A. 关键文件清单

| 文件路径 | 职责 | 行数 |
|---------|------|------|
| `src/main.py` | CLI入口与Python API | 275 |
| `src/core/pipeline.py` | 处理管道 | 596 |
| `src/data/types.py` | 数据类型定义 | 455 |
| `src/data/trajectory.py` | 轨迹管理 | 249 |
| `src/infra/config.py` | 配置管理 | 406 |
| `src/infra/metrics.py` | 性能指标收集 | 228 |
| `src/infra/logger.py` | 日志系统 | 120 |
| `src/infra/exceptions.py` | 异常处理 | 197 |
| `src/core/detector.py` | 检测器 | 272 |
| `src/core/tracker.py` | 跟踪器 | 308 |
| `src/data/loader.py` | 视频加载 | 408 |
| `src/viz/visualizer.py` | 可视化渲染 | 301 |
| `src/export/csv_exporter.py` | CSV导出器 | 301 |
| `src/gui/main_window.py` | GUI主窗口 | 1806 |
| `src/gui/workers.py` | 工作线程 | ~400 |
| `src/gui/controller.py` | GUI控制器 | 209 |
| `src/gui/app.py` | GUI应用入口 | 120 |
| `src/gui/constants.py` | GUI常量配置 | 73 |
| `src/gui/widgets/video_canvas.py` | 视频画布 | ~300 |
| `src/gui/widgets/trajectory_list.py` | 轨迹列表 | 174 |
| `src/gui/widgets/log_panel.py` | 日志面板 | 189 |
| `run_gui.py` | GUI启动脚本 | 38 |

### B. 配置项清单

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `detector.model_path` | str | "yolo11n.pt" | 模型路径 |
| `detector.confidence_threshold` | float | 0.5 | 检测置信度 |
| `detector.iou_threshold` | float | 0.45 | NMS IOU阈值 |
| `detector.device` | str | "auto" | 推理设备 |
| `detector.classes` | list[int] | [0] | 检测类别 |
| `detector.imgsz` | int | 640 | 推理图像尺寸 |
| `tracker.tracker_type` | str | "bytetrack" | 跟踪器类型 |
| `tracker.track_buffer` | int | 30 | 轨迹缓冲帧数 |
| `tracker.match_thresh` | float | 0.8 | 跟踪匹配阈值 |
| `tracker.track_thresh` | float | 0.5 | 跟踪置信度阈值 |
| `tracker.new_track_thresh` | float | 0.6 | 新建轨迹阈值 |
| `visualizer.box_color` | tuple | (0, 255, 0) | 边界框颜色 |
| `visualizer.text_color` | tuple | (255, 255, 255) | 文本颜色 |
| `visualizer.trajectory_color` | tuple | (255, 0, 0) | 轨迹颜色 |
| `visualizer.line_thickness` | int | 2 | 线条宽度 |
| `visualizer.trajectory_length` | int | 50 | 轨迹显示长度 |
| `visualizer.show_center_point` | bool | True | 显示中心点 |
| `visualizer.show_confidence` | bool | True | 显示置信度 |
| `visualizer.font_scale` | float | 0.6 | 字体缩放 |
| `visualizer.show_bbox` | bool | True | 显示边界框 |
| `visualizer.show_trajectory` | bool | True | 显示轨迹 |
| `visualizer.show_id` | bool | True | 显示ID标签 |
| `pipeline.show_progress` | bool | True | 显示进度条 |
| `pipeline.save_output` | bool | True | 保存输出视频 |
| `pipeline.output_fps` | float | None | 输出视频帧率 |
| `pipeline.skip_frames` | int | 0 | 跳帧处理数量 |
| `pipeline.warmup` | bool | True | 模型预热 |
| `logging.level` | str | "INFO" | 日志级别 |
| `logging.format` | str | - | 日志格式 |
| `logging.file` | str | "logs/tracking.log" | 日志文件 |
| `logging.rotation` | str | "10 MB" | 日志轮转大小 |
| `logging.retention` | str | "7 days" | 日志保留时间 |

### C. 数据类型清单

| 类型 | 字段 | 说明 |
|------|------|------|
| `BoundingBox` | x, y, w, h, confidence | 边界框 |
| `Detection` | bbox, class_id, class_name, track_id | 检测结果 |
| `TrackedObject` | track_id, detection, frame_id, timestamp | 跟踪对象 |
| `Frame` | frame_id, timestamp, image, detections, tracked_objects | 帧数据 |
| `Trajectory` | track_id, points, max_length | 轨迹历史 |
| `PerformanceMetrics` | fps, total_frames, avg_detection_time_ms, ... | 性能指标 |

### D. 异常类型清单

| 异常类型 | 触发场景 | 处理策略 |
|----------|----------|----------|
| `TrackingSystemError` | 系统级错误 | 记录错误，退出 |
| `ModelLoadError` | 模型加载失败 | 记录错误，提示下载，退出 |
| `VideoLoadError` | 视频/摄像头无法打开 | 记录错误，退出 |
| `ConfigurationError` | 配置文件格式错误 | 使用默认配置 + 警告日志 |
| `InferenceError` | 单帧推理失败 | 跳过该帧，继续处理 |
| `ExportError` | CSV写入失败 | 记录错误，不中断处理 |

---

**文档结束**

*生成时间: 2026-04-16*
*更新时间: 2026-04-16 (补充controller.py、app.py、tracker.py、loader.py、logger.py、GUI组件分析) *
*分析工具: Sisyphus AI Agent*
*项目版本: 基于最新代码*

---

## 文档更新说明

本次更新补充了以下内容：

1. **删除重复内容** - 删除了重复的问题1（第二次出现的那一段）
2. **补充遗漏模块分析**：
    - 8.13 CSVExporter (CSV导出器)
    - 8.14 GUI启动流程 (app.py + run_gui.py)
    - 8.15 GUI常量配置 (constants.py)
    - 8.16 GUI配置热更新机制详解
    - 8.17 性能指标数据流详解
    - 8.18 测试架构分析
    - 8.19 main.py - CLI入口与Python API
    - 8.20 metrics.py - 性能指标收集器
    - 8.21 constants.py - GUI常量配置
    - 8.22 YAML配置文件详解
    - 8.23 docs/目录文档概述
    - 8.24 修复脚本概述
    - 8.25 controller.py - GUI控制器
    - 8.26 app.py - GUI应用入口
    - 8.27 tracker.py - 跟踪器
     - 8.28 loader.py - 视频加载器
     - 8.29 logger.py - 日志系统
     - 8.30 GUI组件详细分析
     - 8.31 video_canvas.py - 视频画布组件
     - 8.32 visualizer.py - 可视化渲染器
3. **修正数据不一致**：
     - 文件行数：main_window.py 从~800行修正为1806行
     - 测试数量：91个测试用例（7个测试文件）✅ 已验证
     - 新增文件行数：metrics.py (228), logger.py (120), exceptions.py (197), detector.py (272), tracker.py (308), loader.py (408), controller.py (209), trajectory_list.py (174), log_panel.py (189), app.py (120), constants.py (73), run_gui.py (38), main.py (275), video_canvas.py (339), visualizer.py (301)
4. **补充关键逻辑**：
    - GUI配置热更新的详细机制
    - 性能指标数据流说明
    - main.py CLI参数解析与Python API设计
    - metrics.py 滑动窗口FPS计算逻辑
    - constants.py 刷新节流参数配置
    - YAML配置文件详细内容分析
    - controller.py 状态机设计
    - tracker.py ByteTrack封装
    - loader.py 视频加载逻辑
    - logger.py 日志配置
    - video_canvas.py 高质量渲染
    - visualizer.py 可视化渲染
5. **重新评估问题分级**：
    - 问题18（Worker异常静默失败）从🟡修正为🔴
    - 问题19（CSV原子性）从🟡修正为🔴

### 文档统计

- 总字数: ~42,000字 (从40,000字增加)
- 代码示例: 150+个 (从140+个增加)
- 流程图: 5个
- 表格: 40+个 (从35+个增加)
- 问题分析: 19个 (13个已修复 + 6个未修复)
- 模块分析: 32个 (从30个增加)
